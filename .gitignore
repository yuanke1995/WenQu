# 各子工程（wenqu-server / wenqu-web）自带的 .gitignore 已覆盖构建产物与运行期目录，
# 这里的规则只处理跨工程、跨语言的通用项（不带前导斜杠的规则对任意层级生效）。
*.log
.DS_Store

# 编辑器 / IDE
.idea/
*.iml
.vscode/

# Python（scripts/ 下的本地重排服务）
__pycache__/
/.pyenv/

# 本地环境与敏感配置
.env
.env.local
.env.*.local
config/application-local.yml

# 工作区工具目录
/.workbuddy/
