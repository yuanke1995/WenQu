target/

# 运行期产物：均由启动流程从 src/main/resources/skills 与内置样板复制/生成，
# 不随源码演进，不进版本库（与仓库根 .gitignore 的 data/ 同一口径）。
#
# ⚠️ 这些路径在 RuntimePaths 里是**相对 cwd** 的，因此 IDE 与 build.sh 的
# 工作目录必须都是模块根（wenqu-server/），否则会分裂到仓库根。
skill-sources/
skill-projections/
user-data/
data/
logs/
run/
