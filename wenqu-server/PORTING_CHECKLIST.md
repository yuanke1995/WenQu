# 参考实现 → 问渠 移植清单（分支 feature/platform-alignment）

> 参考实现位于本机 `~/Downloads/<参考实现>-main/backend`（完整绝对路径见 `.workbuddy/memory/`），分两大层：
> - `package/<ref>/`（业务逻辑，services/knowledge/agents/repositories…）
> - `server/`（API 层：FastAPI 入口 + 25 个 router + server utils）
>
> 本清单条目与参考文件 **1:1 对应**，路径均以参考根为基准（`<ref>` 为参考实现包名占位）。

## 用法（重要）
- 每搬完 **一个条目**，把行首 `[ ]` 改成 `[x]`，并在文末「移植记录」表追一行（日期 + 条目 + commit 短哈希）。
- 严格照搬参考实现：必要替换（引擎版本 / MySQL 方言 / 缓存 key 前缀 / 品牌前缀）与能力差异（如 LangGraph→StateGraphPort）才允许改动，且必须标注。
- 参考实现产品名、包名等标识词在代码与注释中一律清除（第三方库名如 pypinyin 不算）。
- 参考 `services/viewer_filesystem_service.py`（批次⑳）为对拍范本。

## 本地启动与实跑验收（2026-09-19 首次跑通）

本机无 maven，用 `wenqu-server/build.sh` 代替 `mvn`（javac + `~/.m2` 依赖 jar 拼 classpath）：

```bash
cd wenqu-server
bash build.sh             # 全量编译到 target/classes（每次先清空，见脚本注释）
bash build.sh start       # 编译后【后台】启动，监听 8095，上下文 /v2（日志 run/server.log）
bash build.sh status      # 端口 / PID / 健康检查
bash build.sh stop        # 停止（按端口找监听者，不依赖 pid 文件）
bash build.sh restart     # stop + start
bash build.sh deps-check  # 对账「pom 依赖闭包」与「源码 import」
bash build.sh run         # 【前台】启动（占住终端，仅调试时用）
```

- **别用 `run` 当常驻服务**：它是前台进程，Ctrl+C、关窗口、或另开终端再跑一次，都会制造
  「Port 8095 already in use」的假故障；而后台实例一旦脱离终端，`ps` 未必看得见，
  表现为「明明没跑却又起不来」。`status` / `stop` 按端口判定，不依赖 pid 文件。
- **工作目录必须是模块根 `wenqu-server/`**：`RuntimePaths` 里 `skill-sources` / `skill-projections` /
  `user-data` / `data` 都是相对路径。IDE 默认 cwd 是项目根，会把运行期目录写到仓库根，
  与 `build.sh` 启动产生的副本分裂成两套。已提供 `.idea/runConfigurations/WenquServerApplication.xml`
  （指定 `WORKING_DIRECTORY=$PROJECT_DIR$/wenqu-server` 与固定密钥）。
- ⚠️ **classpath 有两条互不一致的来源**：`build.sh` 用「`~/.m2` 全量」，IDEA / Maven 用「`pom.xml` 闭包」。
  任何躺在 m2 里却没写进 pom 的 jar，都会造成「命令行编得过、IDEA 报找不到符号」。
  凡改动源码引入新依赖，先 `bash build.sh deps-check` 对账再补 pom。

- 前置：MySQL `127.0.0.1:3306/ai_doc_assistant`、Redis `127.0.0.1:6379` 可用；**不需要** Neo4j
  （连接为按需懒建，`Neo4jAutoConfiguration` 已排除）。
- `build.sh run` 已内置固定密钥（`SERVER_PORT` / `WENQU_JWT_SECRET` / `JWT_SECRET_KEY` / `WENQU_INSTANCE_ID`），
  否则每次重启令牌全失效，联调时表现为「刚登录又被踢回登录页」。
- 管理员账户：首次 `POST /v2/api/auth/initialize`（`{"uid":"admin","password":"..."}`）建号，
  之后 `POST /v2/api/auth/token` 登录取令牌。
- **两套并存的登录契约**：`/api/auth/**`（JWT `sub` = `users.id`）与 `/api/ai/auth/**`
  （`sub` = `c_ai_user.uid`），`config/UserContextInterceptor` 两者都认。
- **前端联调口径**：本仓库 `web/` 是旧版前端（代理 `localhost:8090/ai`，指向旧后端），对不上本服务。
  参考前端的开发服务器按 `^/api` → `VITE_API_URL` 代理，故设 `VITE_API_URL=http://127.0.0.1:8095/v2` 即可联调。
- 已知非阻塞现象：`GET /api/system/ready` 返回 **503**（`checks.worker = WorkerUnavailableError`）——
  run 队列 worker 进程未搬，参考实现在无 worker 时同样 503；前端不消费该端点。
- 运行期派生目录 `skill-sources/`、`skill-projections/`、`user-data/`、`data/`（含启动期生成的
  `data/secret/config-rsa.key`）、`logs/`、`run/` 均由启动流程生成，已 gitignore。
- 依赖清单以 **`pom.xml` 为准**：`build.sh` 的全量 classpath 只是「能编过」的宽松近似。
  当前 `deps-check` 的缺口为 0（223 个闭包 artifact、199 条第三方 import 全部有归属）。

## 进度概览
- ✅ 已完成：repositories(26) / models(10) / config(4) / permissions(2) / workspace 前置(3) / common 工具(22) / agents 前置层(13) / knowledge 解析面(17) / storage(minio) / **services 38-43** / 3 个 controller（Auth/Document/KnowledgeBase） / RAGFlow 分块家族 12/12（全量直译，见下） / knowledge 根核心 9/9 + knowledge_task_service + workspace_service / knowledge/eval 4/4 / **knowledge/utils 全 5/5（batch⑩ 补齐 mindmap_utils + sample_question_utils 的高层 DB/LLM 面 → KnowledgeContentService）** / agents/mcp 1/1 / **agents/skills 2/3（service + remote_install）** / **§五 API 层 26/33（routers 19 + utils 5）** / §三 引擎底座（`agents/engine` 4 类 + `AIMessage`/`GraphStateSnapshot`，非照搬项，`eae0a77`） / **§三 middlewares 10/10（批次① 5 + 批次② 1 + 批次③ model_input/skills/subagent_task/summary）** / **§三 toolkits 5/5（批次④：service 全量 + kbs/tools + buildin/install_skill + debug/tools 空文件；批次⑤：buildin/tools）** / **§三 buildin/chatbot + buildin/subagent 5/5（批次⑥ 三个数据类 + 批次⑪ 两个 graph.py 构图装配）** / **§三 callbacks 1/1（批次⑦：model_request_timing → ModelRequestTimingMiddleware）**
- 🔲 剩余：5 大块，**8 项待办 + 1 项部分完成**（§一 2 / §二 2 / §三 0 / §五 4，另 `[~]`：lifespan；2+2+0+4=8）。**§一 agents 服务侧已全部落地（批次⑫~⑰ → agent_run_service / agent_config_service / agent_run_manifest_service / agent_request_queue_service / agent_request_service / scheduled_agent_service / subagent_run_service / artifact_service）**，`agent_router` 与 `scheduled_agent_router` 已落地（批次㉑）；`agent_invocation_call_router` / `agent_invocation_eval_router` 已落地（批次㉒），`agent_invocation_channel_router` 的 §一 阻塞**已全部解除**（`chat_service` 随批次㉓ 落地）→ 可直接搬。**`chat_service` 已随批次㉓ 全量落地**；**`context_compression_service` 已随批次㉔ 落地（外层 + 端口隔离口径）→ `chat_router` 的 §一 阻塞清零**。**服务已能实跑**（启动组件 5/11，见「本地启动与实跑验收」）。
- 📌 §一 剩余 2 项（2026-09-19 复核，批次㉔ 后更新）：`chat_service`（1625 行，流式事件翻译核心）**已全量落地（批次㉓，43/43 顶层函数）**；`context_compression_service` **已随批次㉔ 落地**（外层逐行照搬 + 摘要执行/checkpoint 写入两处能力缺口端口隔离、未装配即抛错）；`run_worker`/`arq_worker` 属 ARQ/引擎面——`run_worker` 依赖 `chat_service` 的三个流入口（`stream_agent_chat` / `stream_agent_resume` / `get_agent_state_view`），**该前置现已具备**。**结论：§一 的「数据面可移植项」至此全部搬完（仅余 ARQ/引擎面两项）。**
- 📌 **§三 引擎底座已落地（2026-09-19，`eae0a77`）**：以 spring-ai-alibaba 的 graph-core / agent-framework 为底座，把参考实现消费的 LangGraph 图 / `create_agent` ReAct 循环 / checkpointer / `astream_events("v3")` 事件词汇桥接为 `agents/engine/`（`GraphFactory`/`GraphPort`/`AgentEventStream`/`GraphCodec`）。**§一 的 `chat_service` / `context_compression_service` 的「引擎面不可用」这一阻塞点因此解除**，但它们自身所需的上层中间件仍在 §三（见下「落地方式」表）。

---

## 一、services 剩余（4）
参考路径前缀 `package/<ref>/services/`
- [x] agent_config_service — agent_config_service.py（AgentConfigService：`prepare_agent_config_write` 的写入口径；`preload_skills` 归并到 `skills`，用 `BaseContext.filterConfigByRole` + `AgentContextService.resolveAgentResourceOptions`）
- [x] agent_request_service — agent_request_service.py（AgentRequestService：`RunOrigin`/`AgentRequestInput` 记录 + `submitAgentRequest`；origin 校验、request_id 幂等三分支、会话创建、Workdir 绑定）
- [x] agent_request_queue_service — agent_request_queue_service.py（AgentRequestQueueService：排队策略与状态常量、steer/取消/续跑、SSE `streamRequestEvents`、`dispatchNextRequest`/`recoverPendingDispatches`/`_dispatchLockedHead` 唯一键竞争判定）
- [x] agent_run_service — agent_run_service.py（AgentRunService：模型/审批解析、`resolveAgentRunConfig`、`buildRunResponse`、事件压缩、`persistAgentRunRecord` 嵌套事务 + 唯一键幂等、`enqueueAgentRun`/`commitAndEnqueue`、`getAgentRunView`/`getAgentRunResult`/`cancelAgentRunView`、SSE `streamAgentRunEvents`（轮询 + 心跳 + `end` 补偿））
- [x] agent_run_manifest_service — agent_run_manifest_service.py（AgentRunManifestService：`MANIFEST_SCHEMA_VERSION=2`、`computeManifestFingerprint`/`computeConfigDigest`、`resolveCodeRevision` 读 `WENQU_CODE_REVISION`、`prepareRunExecution`；依赖 `common/CanonicalJson`）
- [x] subagent_run_service — subagent_run_service.py（SubagentRunService：`SubagentStartResult`/`SubagentRunBusy` 异常（`toPayload` 五键恒定）、`subagentRunUrls`/`serializeSubagentRunState`（键序逐字对齐 + null 过滤）、`start` 三阶段事务拆分（校验+关系+run 落库 → 事务外 `enqueueAgentRun` → 幂等由 request_id 兜底）、`getRunForCreator`/`createRunRecord`/`ensureChildConversation`/`validateThreadRelation`/`ensureThreadRelation`、`translateCreationError`（409 `{code:run_busy}` → `SubagentRunBusy`）。**同时解除 `chat_service` 对该模块的 import 依赖**（`serialize_subagent_run_state`））
- [x] scheduled_agent_service — scheduled_agent_service.py（ScheduledAgentService：校验/幂等/派发/worker 领取全量 21 函数；依赖自实现 `common/CronSchedule`（croniter 等价，能力差异已标注）+ `ScheduledAgentRepository`/`UserRepository.lockActiveByUid`）
- [ ] run_worker — run_worker.py
- [ ] arq_worker — arq_worker.py
- [x] knowledge_task_service — knowledge_task_service.py（KnowledgeTaskService：5 个任务处理函数 + 失败钩子）
- [x] chat_service — chat_service.py（ChatService；**43/43 顶层函数全量落地**，全量直译：输入装配（`_with_attachment_context`）/ Langfuse 上下文与 trace 固化 / Model+Tool 审计器构建 / artifact 路径归一 / agent_state 提取与签名 / token_usage 合并 / 流式事件翻译三件套（`_message_chunk_yuxi_events`→`messageChunkEvents`、`_protocol_event_yuxi_event`→`protocolEventToStreamEvent`、`_message_payload_yuxi_events`→`messagePayloadEvents`）/ `_context_compression_payload` / 消息落库（`_save_ai_message` / `_save_tool_message` / `save_partial_message` / `save_messages_from_langgraph_state` 含 model/tool 审计对账）/ 中断三件套（ask_user_question / tool_approval / pending）/ `_resolve_agent_runtime` / `check_and_handle_interrupts` / **`streamAgentChat`** / **`streamAgentResume`** / `_serialize_state_messages` / `readCheckpointState` / `getAgentStateView`）。**六处平台差异显式标注（类 Javadoc）**：① async generator → `Consumer<String>` 回调 sink（`UncheckedIOException` ≡ `ConnectionError`）② `asyncio.CancelledError` 无 Java 对位（仅保留 ConnectionError 分支）③ 显式 DB 会话 → `TransactionTemplate`④ `model_request_recorder` 参数**并入** `callbacks/ModelRequestTimingMiddleware`（故无 `_persist_model_request_timing` 调用点）⑤ checkpoint 读取改走「解析 Agent → 构图 → `agetState`」（本工程无进程级 checkpointer 入口）⑥ 中断信息取值面：`GraphStateSnapshot.extractInterruptInfo()`（先 `tasks` 后 `values["__interrupt__"]`）。**品牌前缀必要替换**：`"yuxi.context_compression"` → `"wenqu.context_compression"`（生产者/消费者同处后端、前端零引用，与 `AgentRunService.AGENT_STATE_EVENT_NAME` 同口径；两侧契约已在 producer/consumer 注释中互相指向））
- [x] context_compression_service — context_compression_service.py（ContextCompressionService；**7/7 模块级函数全量落地**：`compress_thread_context` / `_ensure_thread_idle` / `_compress_agent_checkpoint_in_runtime` / `_ensure_runtime_available` / `_release_runtime` / `_compress_agent_checkpoint` / `_with_compression_usage`。**能力差异显式标注（类 Javadoc，6 条）**：① **摘要执行能力未装配** —— `compressor.aforce_summarize(values)` 骨架全在 langchain `SummarizationMiddleware` 基类（`_apply_event_to_messages` / `_determine_cutoff_index` / `_partition_messages` / `_aoffload_inline_media` / `_get_session_id` / `_build_new_messages_with_path` / `_count_tokens`），本工程未照搬该基类、引擎面 `SummarizationHook` 语义不等价 → 收敛为 `CheckpointCompressor` **端口**，未装配即抛错（**绝不静默返回空 update**）；② **checkpoint 写入能力未装配** —— 参考经 `CompiledStateGraph.aupdate_state`，本工程 `AgentsGraphPort` 只有 `agetState` → 收敛为 `CheckpointStateWriter` **端口**；③ 无显式 DB 会话 / 无 `db.commit()` 调用点（`TransactionTemplate` 圈定「加锁→校验→压缩→提交」边界，`PROPAGATION_REQUIRED`，与 `ChatService` 同口径）；④ `asyncio.to_thread` 无 Java 对位（同步直调）；⑤ 摘要阈值对非数值回落 `DEFAULT_SUMMARY_THRESHOLD_K`（参考在「属性存在但为 None」时 `None * 1024` 抛 `TypeError`）；⑥ `_with_compression_usage` 的 `dict(result)` 前提保持（update 空则跳过写入）。**配置面照搬**：`agent.context_schema()` + `update_config(config_json['context'])` 走 `AgentRunService.loadAgentRunContext`（既有等价物），`resolve_agent_run_model_spec(extra_metadata["model_spec"], context.model)` 走 `AgentRunService.resolveAgentRunModelSpec`。**参考单测 `test/unit/services/test_context_compression_service.py` 的五组断言已逐条纳入实现**：事件序 lock→idle→runtime→compress→release→commit、压缩失败仍 release 且不掩盖原错误、provisioning 失败时 release 抛错也不掩盖原错误、`_with_compression_usage` 的**键序与过滤集**（剔除 `TOKEN_USAGE_CONTEXT_FIELDS`、保留 `thread` 桶、追加 compression/summary_active/summary_trigger_tokens，阈值 = `summary_threshold * 1024`）、非空闲三态（active_run / interrupted latest_run / queued）一律 409 `thread_busy`）。**配套补口**：`BaseAgent.getCapabilities()`（capabilities 的跨包读取出口，与既有 `resolveContextSchema()` 同体例）+ `TokenUsageMiddleware.TOKEN_USAGE_CONTEXT_FIELDS` 提升为 `public static final`（跨包只读））
- [x] artifact_service — artifact_service.py（ArtifactService：线程 artifact 下载与保存全量 6 函数 —— `_normalize_artifact_path`（workdir/virtual/skills 三根白名单 + `..` 拒绝）、`_require_skill_artifact_access`（Skill 可见性重校验）、`_copy_skill_file_to_path`（`SafeFiles.openRegularFile` no-follow 有界复制）、`_copy_artifact_to_path`（403/400/413/404 四类映射）、`resolve_thread_artifact_view`（下载/预览/预览超限）、`save_thread_artifact_to_workspace_view`（目标校验 + 同名递增候选名 + 409 空间耗尽）。依赖面 `FilePreviewService`/`WorkdirService`/`SkillService`/`SafeFiles`/`BackendPaths`/`Workspace` 全部就位，**无 langchain/langgraph 依赖 → 可直搬**）
- [x] workspace_service — workspace_service.py（WorkspaceService：9/9 公开函数全搬 —— search/list_tree/read_bytes/read_content/write_content/delete/create_directory/upload/download）

## 二、knowledge 核心（约 40，parser 子包与 kb_utils/pdf_utils 已搬）
参考路径前缀 `package/<ref>/knowledge/`
### 根核心（9）
- [x] base — base.py（KnowledgeBaseRuntime + KnowledgeBaseException）
- [x] cache — cache.py（repositories/KnowledgeBaseCache，本会话前已搬）
- [x] factory — factory.py（KnowledgeBaseFactory，区别于 parser/factory.py）
- [x] manager — manager.py（KnowledgeBaseManager）
- [x] preview — preview.py（KnowledgeFilePreviewService）
- [x] read_models — read_models.py（KnowledgeBaseConfig / KnowledgeBaseSummary / KnowledgeBaseDetail）
- [x] runtime — runtime.py（KnowledgeBaseRegistrar，Spring 启动时注册）
- [x] schemas — schemas.py（KbToolSchemas）
### chunking/ragflow_like（12）
- [x] dispatcher — chunking/ragflow_like/dispatcher.py（RagflowChunkDispatcher）
- [x] nlp — chunking/ragflow_like/nlp.py（RagflowNlp）
- [x] presets — chunking/ragflow_like/presets.py（service/ChunkPresets，本会话前已搬）
- [x] parsers/book — chunking/ragflow_like/parsers/book.py（BookChunkParser）
- [x] parsers/general — chunking/ragflow_like/parsers/general.py（GeneralChunkParser）
- [x] parsers/laws — chunking/ragflow_like/parsers/laws.py（LawsChunkParser）
- [x] parsers/qa — chunking/ragflow_like/parsers/qa.py（QaChunkParser）
- [x] parsers/semantic — chunking/ragflow_like/parsers/semantic.py（SemanticChunkParser；markdown-it→RagflowMdTokenizer 纯扫描近似，bs4→RagflowTableUtils 纯字符串解析，embed_fn 降级按 token 合并）
- [x] parsers/separator — chunking/ragflow_like/parsers/separator.py（SeparatorChunkParser）
- [x] utils/md_parser — chunking/ragflow_like/utils/md_parser_utils.py（RagflowMdParserUtils）
- [x] utils/semantic — chunking/ragflow_like/utils/semantic_utils.py（RagflowSemanticUtils；聚类用 sklearn → 降级按 token 合并）
- [x] utils/table — chunking/ragflow_like/utils/table_utils.py（RagflowTableUtils；bs4 → 纯字符串解析）
### eval（4）
- [x] metrics — eval/metrics.py（knowledge/eval/EvalMetrics：RetrievalMetrics / AnswerMetrics / EvaluationMetricsCalculator；json_repair.loads → common/LooseJson 近似，能力差异已标注）
- [x] evaluator — eval/evaluator.py（knowledge/eval/EvalEvaluator：normalize_query_result / build_answer_prompt / generate_answer_if_needed / evaluate_question / aggregate_metrics；检索面走 `kbManager.aquery`）
- [x] benchmark_generation — eval/benchmark_generation.py（knowledge/eval/EvalBenchmarkGeneration：collect_kb_chunks / select_neighbor_chunks_by_kb_query / select_graph_enhanced_chunks（走 MilvusGraphService.queryAndRankChunksByPpr）/ build_benchmark_generation_prompt / iter_generated_benchmark_items；asyncio Queue → 线程池 + 按下标 Future.get 保序）
- [x] service — eval/service.py（knowledge/eval/EvalService：数据集 upload/list/detail/export/delete/generate/resume + 评估 run/list/results/delete + 两个长任务体 generateDatasetTask/runEvaluationTask；任务 Handler 见 EvalTaskService）
### graphs（6）
- [x] extractors/base — graphs/extractors/base.py（extractors/GraphExtractor）
- [x] extractors/factory — graphs/extractors/factory.py（extractors/GraphExtractorFactory）
- [x] extractors/llm — graphs/extractors/llm.py（extractors/LlmGraphExtractor）
- [x] graph_utils — graphs/graph_utils.py（GraphUtils）
- [x] milvus_graph_service — graphs/milvus_graph_service.py（MilvusGraphService）
- [x] milvus_graph_vector_store — graphs/milvus_graph_vector_store.py（GraphVectorStore；后端由 Spring AI VectorStore 承载，能力差异已标注）
> 以上 6 项 + storage/neo4j 由 `688abda` 一次搬完（含 KnowledgeGraphRetrieval ← implementations/milvus.py 的检索融合函数）。
### implementations（4）
- [ ] dify — implementations/dify.py（参数面已搬入 KnowledgeBaseTypeParams；aquery 依赖外部 Dify HTTP 接口，未搬）
- [x] milvus — implementations/milvus.py（执行器由既有 Spring 内核 VectorStore / KnowledgeChunkRepository / HybridRetrievalService 承载；类型参数与检索参数清单 → KnowledgeBaseTypeParams）
- [ ] notion — implementations/notion.py（参数面已搬入 KnowledgeBaseTypeParams；aquery 依赖外部 Notion HTTP 接口，未搬）
- [x] read_only_connectors — implementations/read_only_connectors.py（ReadOnlyConnectors：只读能力判定与报错文案）
### utils 剩余（5，kb_utils/pdf_utils 已搬）
- [x] mindmap_utils — utils/mindmap_utils.py（纯函数面 KnowledgeMindmap；**高层 DB/LLM 面**全部搬入 `knowledge/KnowledgeContentService`：`listMindmapFilesPage`/`loadMindmapCurrentFiles`/`getMindmapDatabaseFiles`/`getMindmapDiff`/`updateMindmapIncremental`/`generateDatabaseMindmap`/`getMindmapDatabasesOverview`/`getDatabaseMindmapData`/`removeFileFromMindmap`/`batchRemoveFilesFromMindmap`）
- [x] sample_question_utils — utils/sample_question_utils.py（纯函数面 KnowledgeSampleQuestions；**高层面**搬入 `knowledge/KnowledgeContentService`：`generateDatabaseSampleQuestions`/`getDatabaseSampleQuestions`）
- [x] security — utils/security.py（KnowledgeSecurity）
- [x] url_fetcher — utils/url_fetcher.py（KnowledgeUrlFetcher）
- [x] url_validator — utils/url_validator.py（KnowledgeUrlValidator；白名单环境变量已去品牌化为 `WENQU_URL_WHITELIST`）

## 三、agents 运行时（约 32，前置层 + 引擎底座已搬）
> 已搬：context.py→BaseContext / state.py→AgentState / tool_approval.py→ToolApproval / backends/paths.py→BackendPaths / chatbot/prompt.py→ChatbotPrompt / skills/repository.py→SkillRepository / toolkits/registry.py→ToolkitsRegistry / toolkits/utils.py→ToolkitsUtils
>
> **另新增「引擎底座」**（参考实现无对应文件 —— 它是 LangGraph / DeepAgents / LangChain 三套第三方框架的 Java 等价物，见下 `engine` 小节）。
> 底座落地后，本节剩余条目由「硬阻塞」转为「可逐条照搬」，落地方式见各条目旁注。

参考路径前缀 `package/<ref>/agents/`

### engine（新增，非照搬项：第三方框架的 Java 等价底座）
- [x] 图引擎桥接 — `agents/engine/`（**GraphFactory / GraphPort / AgentEventStream / GraphCodec**）+ 承载类 `agents/AIMessage`、`agents/GraphStateSnapshot`：以 spring-ai-alibaba 的 graph-core / agent-framework 为底座，对齐参考实现的「LangGraph 图 + `create_agent` ReAct 循环 + 中间件链 + checkpointer + `astream_events(version='v3')` 事件流」。映射逐条：`create_agent(...)` → `GraphFactory.builder()`；`StateGraph`/`CompiledGraph` → `GraphPort`；`AsyncPostgresSaver` → checkpointer（本工程 MySQL）；`AgentMiddleware` → `Hook`（BEFORE/AFTER_AGENT、BEFORE/AFTER_MODEL）+ `ModelInterceptor`（≈`wrap_model_call`）/ `ToolInterceptor`（≈`wrap_tool_call`）；`astream(stream_mode='messages')` → `GraphPort.astreamMessages`；`astream_events("v3")` → `GraphPort.astreamEvents`；`Annotated[list, merge_artifacts]` → 状态键策略。类名按职责命名，不含实现框架名（框架信息只在类注释的「实现底座」说明里）。**事件数据源必须是节点输出的 `StreamingOutput.message()`** —— `state()` 是共享可变引用，实测同一节点多次产出的内容随读取时机变化，不可用于取消息。能力差异 5 类（引擎 CompileConfig 恒带默认 MemorySaver 致 `hasCheckpointer` 恒真、`recursionLimit` 为编译期配置、tools 事件 `input` 恒为空表、`values` 只在图结束产出一次、对无 checkpoint 的 thread `updateState` 显式抛错）均已逐条标注在各实现类注释。验证：367 源文件 javac 0 错误；stub 模型端到端验证台 **40/40 PASS**（构图与懒编译 / recursionLimit 透传 / ReAct 循环含真实工具执行 / checkpointer 跨调用 / 事件词汇与载荷形状 / 编解码往返）。`eae0a77`

### root（1）
- [x] base — base.py（**BaseAgent**：4 个模块级纯函数 `jsonSafe`/`normalizeToolEventData`/`subagentRouteForNamespace`/`recursionLimitFromContext` + `get_info`/`stream_messages`/`_stream_input_with_state`/`stream_messages_with_state`/`stream_resume_with_state`/`invoke_messages`/`reload_graph`/`get_graph`(抽象)/`_get_checkpointer`/`load_metadata`。**图引擎收敛为 `AgentsGraphPort` 端口**（astreamMessages/astreamEvents/ainvoke/agetState），与 `AgentStateRepository.StateGraphPort` 同口径；端口**无实现**（引擎未照搬）。语言差异：Python 异步生成器 → 回调 sink；`asyncio.create_task` 并发收集子智能体路由 → 后台线程 + `ConcurrentHashMap` + `interrupt` 收尾；2 元组产出 → `{"message":…,"metadata":…}`；`hasattr(model_dump)` → `ModelDumpable` 接口；`resolve_agent_resource_options` → `AgentResourceOptionsResolver` 接缝。新增承载类 `ModelDumpable`/`ToolMessage`/`GraphCommand`）
### backends（6，paths.py 已搬）
- [x] composite — backends/composite.py（**从「配置载体」升级为真接线**：`YuxiFilesystemMiddleware` 提为顶层 `ToolInterceptor`，`interceptToolCall` 逐行复刻 `wrap_tool_call` 分支序（取结果 → 豁免集放行 → 预算开关放行 → 裁剪），裁剪委托框架 `LargeResultEvictionInterceptor`（＝ `_intercept_large_tool_result`；委托时把下游 handler 换成「返回已算好的结果」以避免工具重复执行）。豁免集 `_TOOL_RESULT_EVICTION_EXEMPT_TOOLS` = 框架 `excludeFilesystemTools()` 声明的 6 项（ls/read_file/write_file/edit_file/glob/grep，**不含 execute**）∪ Yuxi 增量 `open_kb_document`，两层判定同集。新增 `SandboxFilesystemBackendAdapter implements FilesystemBackend`（结构化 DTO → 字符串协议投影；**框架 `LARGE_RESULTS_DIR` 硬编码 `user.dir/large_tool_results/` 不可覆写 → 在 I/O 边界重映射到 `<artifacts_root>/large_tool_results/`**，否则写入必落在沙盒授权根外退化成「有裁剪消息、无落盘」）。`_BackendScope` 补 `artifactsRoot()` / `AgentCompositeBackend.artifactsRoot(context)` 显式出口；`createBackend` 返回具体类型 `ProvisionerSandboxBackend`。`CompositeBackend` 未照搬**无语义损失**：参考实现 `routes={}` 为空路由表）
- [x] knowledge_base_backend — backends/knowledge_base_backend.py（KnowledgeBaseBackend：`resolve_visible_knowledge_bases_for_context` 单函数 → `@Service`，注入 KnowledgeBaseManager；`setattr(context,"_visible_knowledge_bases",…)` → `BaseContext.setDynamic`（本工程 `set()` 只写已声明字段，动态属性单列一表避免静默失效））
- [x] sandbox/backend — backends/sandbox/backend.py（**ProvisionerSandboxBackend 全量 ~1000 行**：常量/错误文案、路径与权限纯逻辑（normalizePath/isSameOrChild/pathOverlapsRoot/globForSearchRoot/canReadPath/canListPath/canWritePath/readableSearchPaths/filterReadable*）、文件分类（readFileKind/getFileType/readWindow）、结果塑形（textReadResult/normalizeReadContent/looksLikeBinary/isUtf8DecodeFailure/isMissingFileError/raiseAuthorizedPathOperationError/describeReadError/stripErrorPrefix）、read/execute/ls/write/edit/grep（含 superGrep 进程内实现）/glob/uploadFiles/downloadFiles、安全脚本生成（ensureParentDirectory/buildSnapshotScript/metadataScript/buildUploadScript/buildListScript/buildMkdirScript/buildDeleteScript）与 12 个结果 DTO；I/O 动词经 SandboxRuntimeClient 接口隔离、未部署时 buildClient 抛 SandboxProvisionerException。构造签名 (provider, threadId, uid, workdirPath, createIfMissing, inheritEnv) —— **偏离标注**：参考实现为关键字参数 `(thread_id, *, uid, inherit_env=True, create_if_missing=True, workdir_path=None)`，Java 无关键字参数，故把带默认值的 `workdirPath` 提到第 4 位、两个 boolean 后置，语义与默认值不变 + overrideConnection/overrideClient 测试钩子）
- [x] sandbox/download — backends/sandbox/download.py（**SandboxDownload**，批次⑧ `ab6e006` 已落地并验证台 17/17 PASS）
- [x] sandbox/provider — backends/sandbox/provider.py（**ProvisionerSandboxProvider 全量 ~290 行**：sandboxProvisionerToken/sandboxIdForThread（SHA-256 前 12 位）/_sandbox_key/normalize_env/loadUserAgentEnv（→ AgentEnvRepository.getByUid）/get/release/shutdown/单例 holder；弱引用 GC 语义标注为能力差异（ConcurrentHashMap 强引用替代 WeakValueDictionary）；配套 SandboxConnection/SandboxIdentityMismatchError/SandboxRuntimeClient + 4 个错误类型（PermissionError/IsADirectoryError/FileNotFoundError/FileTransferLimitError）。AgentCompositeBackend.createBackend 改为 new ProvisionerSandboxBackend(sandboxProvider, ...)）
- [x] sandbox/provisioner_client — backends/sandbox/provisioner_client.py（**SandboxProvisionerClient** + `SandboxRecord`：health/create/discover/touch/delete 五端点逐字对齐，JDK HttpClient 替换 httpx；未部署 provisioner 属能力差异，调用时才失败）
### buildin/chatbot（3，prompt.py 已搬）
- [x] context — buildin/chatbot/context.py（**ChatBotContext**：`subagents` 字段（list[str]|None，default None）逐字对齐 name/options/description/type/kind metadata；经 `BaseContext.declareExtraField` 在构造期登记，不污染基类声明表。`subagents` 属「空集合语义」字段 —— null=启用全部可见子智能体，空列表=一个都不启用，与 `AgentContextFields.EMPTY_ALL_CONTEXT_FIELDS` 一致）
- [x] graph — buildin/chatbot/graph.py（**ChatbotAgent**：类属性 name/description/capabilities/context_schema 逐字；`get_graph` 逐行对位 —— `_runtime_prepared` 校验 → `sync_agent_context_skills` → `create_agent_composite_backend` → `load_chat_model(resolve_chat_model_spec(context.model))` → `resolve_configured_runtime_tools` → `build_prompt_with_context` → `_build_middlewares` → `_get_checkpointer`。**中间件一列拆成 hooks(Steer) + interceptors(其余原序)**：filesystem → skills(模型面+工具面) →（memory）→（subagent）→ summary → `TodoListInterceptor(systemPrompt=TODO_MID_PROMPT)` → `PatchToolCallsInterceptor` → `NetworkRetryMiddleware(model_retry_times)` → model_input → token_usage。**能力差异 3 处已标注**：① 审批中间件未注册（无 HumanInTheLoopMiddleware 对位，仅保留 `createToolApprovalInterruptOn` 返回值形状并仍做 normalize 边界校验）② 压缩事件通道未接线（`CompressionEventSink` 传 null，emit 对 null 安全）③ `state_schema=ChatBotState` 无对应传参（引擎固定 messages+AppendStrategy，额外键随 middlewares 注册 KeyStrategy）。全量 407 源文件 0 错误）
- [x] state — buildin/chatbot/state.py（**ChatBotState**：`SubAgentRunState` 键集（13 键序逐字）+ `status` Literal 枚举（7 值序逐字）+ `merge_subagent_runs` reducer（按 `run_id` 合并同一条记录、无 `run_id` 直接追加、不按工具调用 id/子线程 id 兼容匹配）全量逐字照搬。**验证台 10/10 PASS**。该 reducer 正对应 `SubAgentMiddleware` 产出的 `Command(update={"subagent_runs":[...]})`）
### buildin/subagent（2）
- [x] context — buildin/subagent/context.py（**SubAgentContext**：`parent_thread_id`（None，非可配置/隐藏）+ `is_subagent_runtime`（False，非可配置/隐藏）逐字对齐；`is_subagent_runtime` 供 `SkillInstallTool.runInstallTask` 拒绝在子智能体中安装）
- [x] graph — buildin/subagent/graph.py（**SubAgentBackend** + **SubAgentToolFilterMiddleware**：类属性与 `get_info` 的 tools.options 过滤逐字（过滤用**基础集**，敏感工具仍列出、只在运行期隐藏/拒绝）；`get_graph` 逐行对位，构图期即用 `_filter_disabled_tools` 摘掉禁用工具。**中间件一列无 Steer/Memory**（与参考一致），hooks 为空；`NetworkRetryMiddleware()` 走默认 max_retries。`_SubAgentToolFilterMiddleware` 因 Java 单继承拆成**模型面 + 工具面（`asToolInterceptor()`）两个注册对象**，工具面按 `ToolCallResponse.error` 回 `status="error"` 拒绝显式传入的禁用调用（工具列表隐藏不构成执行边界）。配套 `AgentChatModel`（`resolve_chat_model_spec` 逐字 + `load_chat_model` 调用面）、`AgentGraphSupport`（纯抽取的 5 个公共小工具）、`SummaryMiddleware.createFromContext`（对应 `create_summary_middleware_from_context`）、`AgentManager` 改由 Spring 装配实例。全量 407 源文件 0 错误）
### callbacks（1）
- [x] model_request_timing — callbacks/model_request_timing.py（**ModelRequestTimingMiddleware**，`agents/callbacks/` 新包；`ModelInterceptor` 等价 LangChain `BaseCallbackHandler`；首次 `interceptModel` 捕获 `utcNowNaive()` → `AgentRunRepository.recordFirstModelRequest` write-once；状态存 `BaseContext` 动态字段 `_first_model_request_at` 实现 per-run 隔离，兼容单例中间件；异常仅 warning 不阻断。验证台 4/4 PASS。**待接线**：需并入引擎底座 `GraphFactory`/`GraphPort` 的 `interceptors(List)` 链，与现有 middlewares 同批）
### mcp（1）
- [x] service — mcp/service.py（**全量 671 行**：`McpService`（内置同步 / 客户端装配 `McpClientBundle` / 工具缓存与统计 / 配置 CRUD / 启用开关 / 工具开关 / 统一入口三函数）+ `McpTool`（langchain tool 最小面：name/description/args_schema/可变 metadata/handle_tool_error + 调用）+ `McpServerViews`（`to_dict`/`to_mcp_config`/`serialize_mcp_server` 与 JSON 列反序列化）+ `MCPServerNotFoundException` / `McpBuiltinImmutableException`；`MCPServerRepository` 逐条对位参考实现里出现过的查询；启动组件见 `config/McpStartupInitializer`（对应 lifespan 的 `builtin_mcp_servers`，required=False）。**跨语言对拍**：`to_camel_case` / `json.dumps(sort_keys,ensure_ascii,separators)` / `sha256[:16]` 29/29 行逐字节一致）
### middlewares（10/10，批次① `77ca2e3` 5 个 + 批次② `25a35c7` 1 个 + 批次③ 剩余 4 个）
- [x] context — middlewares/context.py → `ContextAwareInterceptor`（合并 @dynamic_prompt + @wrap_model_call）
- [x] dynamic_tool — middlewares/dynamic_tool.py → `DynamicToolMiddleware`（工具筛选 + 哨兵）
- [x] memory — middlewares/memory.py → `MemoryMiddleware`（3 受限工具 + ThreadLocal ToolRuntime）
- [x] network_retry — middlewares/network_retry.py → `NetworkRetryMiddleware`（预算/次数双轨重试）
- [x] steer — middlewares/steer.py → `SteerMiddleware`（jump_to 同形）
- [x] token_usage — middlewares/token_usage.py → `TokenUsageMiddleware`（纯函数聚合数学 37/37 PASS）
- [x] model_input — middlewares/model_input.py（**ImageInputCompatibilityMiddleware**：`_bridge_openai_tool_images` 图片外挂桥接 / `_read_file_image_paths` / `_ocr_fallback_response`（OCR 工具调用 + UUID 工具调用 id）/ `_has_image` / `_is_image_input_rejection`（状态码集合 {400,404,415,422} + 图片词 × 拒绝词双表命中）逐字对齐。必要替换：`request.model` 无对位 → `isOpenAiCompatModel(ModelRequest)` 读 context 的 `openai_compat_model`（同 `ContextAwareInterceptor.resolveModelSpec` 的扩展点口径）；`content_blocks`→消息归一化；`additional_kwargs["read_file_path"]`→`Message.getMetadata()`。能力差异：`ocr_parse_file` 工具本体在 `toolkits/buildin/tools.py` 且依赖未搬的 `backends/sandbox` → 只保留工具名常量与 `getToolNames()` 声明（同 dify/notion/skill_remote_install 口径）；图片块以 Spring AI `Media` 承载）
- [x] skills — middlewares/skills.py（**SkillsMiddleware**：提示词注入（lazy 摘要段 + preloaded 完整说明）/ 依赖展开 / 门控 / `read_file` 命中 SKILL.md 的动态激活全部对位，共 17 个参考函数逐条落地。承载点：`awrap_model_call`→`interceptModel`；`awrap_tool_call`/`wrap_tool_call`→内部类 `SkillActivationInterceptor`（Java 的 Model/Tool 拦截器是两个基类，故拆主类+内部类）。必要替换：`SKILLS_SYSTEM_PROMPT` 三占位符重建、`append_to_system_message`、`_skill_runtime_snapshot`→`BaseContext.getDynamic`、`request.tools`→工具名列表、`get_enabled_mcp_tools`→`McpService.getEnabledMcpTools`、`get_all_tool_instances`→`ToolkitsRegistry.getAllToolInstances`、PurePosixPath→`PosixPathLite`。能力差异：`Command(update={"activated_skills":...})` 写回 → 经 `ToolCallResponse` metadata 回传由构图方落 state（与 token_usage 同一解法）；`asyncio.gather` → 同顺序串行；`_bind_active_mcp_tool` 无对位（工具按名解析））
- [x] subagent_task — middlewares/subagent_task.py（**SubAgentMiddleware**，参考类名 `YuxiSubAgentMiddleware` 去品牌前缀：工厂 `create`（按 uid + `subagents` 配置加载可见子智能体，无可用项返回 null）+ `TASK_SYSTEM_PROMPT` 注入 + 5 个工具 `task`/`subagent_start`/`subagent_status`/`subagent_cancel`/`subagent_await`（参数描述与 JSON Schema 逐字）；纯函数 `_task_result_response` / `_task_wait_timeout_response` / `_json_tool_command` / `_tool_result_with_thread_id` 全部对位。依赖面 `SubagentRunService` / `AgentRunService` / `InputMessageService` / `AgentRepository` / `UserRepository` / `TERMINAL_RUN_STATUSES` 均已就位。必要替换：品牌词→「问渠」、`StructuredTool`→`ToolCallback`、`runtime.tool_call_id`→`bindToolCallId`。能力差异：`Command` 写回 → `ToolCommand` 载体经 `drainPendingCommands()` 落 state；工具 Schema 为编译期常量（天然满足参考单测「不逐 Run 重新推导」））
- [x] summary — middlewares/summary.py（**SummaryMiddleware**，参考类名 `YuxiSummarizationMiddleware`：确定性部分全量逐字照搬 —— 工具结果落盘+预览（`_build_tool_result_preview` / `_structured_search_preview` / `_generic_tool_result_preview` / `_clip_search_content` / `_tool_result_path` / `_write_tool_result` / `_replace_tool_message_content` / `_should_offload_tool_message`）、工具入参截断（`_truncate_ai_tool_call_args` / `_truncate_tool_call_args` / `_truncate_provider_tool_calls` / `_truncate_string_arg`）、触发判定（`_entry_trigger_tokens` / `_trigger_clause_met` / `_should_summarize`）、状态更新与事件（`_build_summary_event` / `_build_state_update` / `_report_offload_result` / `_compute_state_cutoff`）。必要替换：`_TOOL_RESULT_SAVED_MARKER` 品牌词→`wenqu_tool_result_saved`（内部标记不落库）、backend.write→`ToolResultBackend` 端口、`count_tokens_approximately`→`TokenUsageMiddleware.countTokensApproximately`、`get_stream_writer`→`CompressionEventSink` 端口。能力差异：基类提供的摘要生成/历史落盘/截断下标/分区/媒体卸载收敛为 `SummaryGenerator` + `HistoryOffloader` 接口（未注入时只做入参截断+工具结果压缩）；`ContextOverflowError` 用 `isContextOverflow` 近似；`ExtendedModelResponse(command=...)` → `drainStateUpdate()`；`aforce_summarize` 未搬）
### skills（3，repository.py 已搬）
- [x] remote_install — skills/remote_install.py（**纯函数面全量**：`_normalize_source` / `_normalize_skill_name` / `_clean_cli_output`（ANSI + 控制序列 + 装饰符清洗 + `str.splitlines` 语义）/ `_parse_available_skills` / `_parse_search_skills` / `allowed_hosts`（OptionsService.REMOTE_SKILL_SOURCE_POLICY）；常量 7 项逐字对齐。**能力缺口（已标注）**：三个沙盒入口（list/prepare/search）保留签名与非法来源校验后抛 `SkillRemoteExecutionUnavailableException`（外置 provisioner 沙盒未部署，同 dify/notion 口径）；`search("")` 与「skills 列表为空」等参考实现不依赖沙盒的分支照旧生效）
- [x] runtime — skills/runtime.py（**SkillRuntime**：批次⑫ 已搬入 `AgentContextService`/`SkillRuntime`/`AgentManager`，清单曾误标 `[ ]`；本回合核实 `SkillRuntime.java` 已存在，更正标记）
- [x] service — skills/service.py（**全量 1827 行 → 2699 行 Java**：90 个参考函数 90/90 对位（仅 `_user_skills_file_lock` 因 Java 无上下文管理器而落为回调式 `withUserSkillsFileLock`）；常量（slug 正则 / 26 项文本扩展名白名单 / 内置操作者与管理员角色 / 默认与内置共享配置 / 草稿 TTL 3600 / 个人来源类型 / 存储锁 `0x5958534B`）逐字一致；内置 skill 清单 5 条（image-gen/html-preview/deep-research/knowledge-base/mysql-reporter 的 slug + description + version + 三类依赖）逐字对齐；必要替换 3 类（锁键前缀 `wenqu:skills:user-projection:v1:`、PG 事务级建议锁→MySQL 会话级 `GET_LOCK`/`RELEASE_LOCK`、内置描述品牌词→「问渠」）与能力差异 8 类（AsyncSession 形参 / to_thread / gather / fcntl.flock / threading.Lock / to_dict / 内置目录 classpath 化 / YAML）全部已在类注释标注。**内置 skill 资源**复制到 `src/main/resources/skills/`（9 文件，`__init__.py` 除外））
### toolkits（5，registry.py/utils.py 已搬；批次④ 落地 4/5）
- [x] buildin/install_skill — toolkits/buildin/install_skill.py（**SkillInstallTool**：`SANDBOX_PATH_HINT` 与工具描述逐字；`_run_install_task` 全量（子智能体拦截 / 会话信息校验 / 空来源校验 / 沙盒路径分支 / Git 分支要求 skill_names / 远程批量安装 / 写回智能体配置 / 六类结果文案「已安装 Skill: 」「Skill 路径: 」「安装失败 (…)」「Skill 已安装，但当前 Agent 配置未更新，请手动启用」「未发现需要安装的 Skill」「安装异常：」）。必要替换：`ToolRuntime`+`InjectedToolCallId` → ThreadLocal 绑定；`TemporaryDirectory`→`Files.createTempDirectory`。能力差异：`Command(update={"messages":[...]})` → 返回 content 文本；沙盒路径下载与远程安装（`prepareRemoteSkillsBatch` 三入口）因 `backends/sandbox` 未搬不可用 —— 保留 slug 与路径白名单校验，到下载步抛错并被外层捕获成「安装异常：…」，与参考实现 ValueError 的形状一致）
- [x] buildin/tools — toolkits/buildin/tools.py（**BuildinTools**：`web_search`（豆包链路全量 —— `_build_doubao_search_payload` 的 Query 截断 100 / Count 夹取 1-50 / Sites 上限 20 用 `|` 连接 / BlockHosts 上限 5 / ContentFormats、`_parse_doubao_search_response` 的 Error 分支与 WebResults 字段、HTTP 15s 超时）、`present_artifacts`（路径白名单 + Command 载体 `ArtifactCommand`）、`ocr_parse_file`（`_resolve_ocr_source_path` 的 `..` 穿越校验 + `_next_ocr_output_path` 重名 `-1/-2` + `_safe_ocr_output_stem` 清洗 + `_ocr_preview` 1200 字截断）、`ask_user_question`（`normalize_questions` + interrupt 载荷 `{"questions":…,"source":"ask_user_question"}`）。必要替换：`httpx`→JDK `HttpClient`、`os.getenv`→`System.getenv`、`ToolRuntime`/`InjectedToolCallId`→ThreadLocal、`Command`/`interrupt`→载体。能力差异：Tavily 供应商无实现（`langchain_tavily`）；沙盒文件存在性校验与 OCR 解析（`services/ocr_service.py`）未搬 —— 保留全部纯函数校验，到沙盒/OCR 步抛错。**验证台 30/30 PASS**）
- [x] debug/tools — toolkits/debug/tools.py（**参考实现为空文件**，无需照搬；`debug/__init__.py` 亦无内容）
- [x] kbs/tools — toolkits/kbs/tools.py（**KnowledgeTools**：7 个工具 `list_kbs`/`get_mindmap`/`query_kb`/`open_kb_document`/`find_kb_document`/`search_file`/`download_kb_file` 全量，分类 `knowledge`、标签 `[知识库]`、展示名逐字对齐；共享 helper `_resolve_visible_knowledge_bases_for_query`/`_find_query_target`/`_runtime_sandbox_scope`/`_resolve_download_output_path`（重名 `_1/_2` 递增）逐字。依赖面 `KnowledgeBaseManager.retrieve/openDocument/findInDocument/searchDocumentFiles/getFileDownload/databaseTypeSupportsDocuments` + `KnowledgeBaseBackend` + `KnowledgeBaseRepository` 全部就位。必要替换：`@tool(...)` 装饰器→显式 `ToolExtraMetadata` 注册；`ToolRuntime`→ThreadLocal 绑定；pydantic schema→JSON Schema 常量。能力差异：`download_kb_file` 的沙盒落盘不可用（同 install_skill 口径）；工具返回 dict/list → JSON 字符串（字段结构一致））
- [x] service — toolkits/service.py（**ToolkitsService** 补齐 `resolve_configured_runtime_tools`：基础工具（`context.tools`，按 `category="buildin"` 取，缺失 warn 跳过）→ MCP 工具（`context.mcps` 去重保序，取「启用中的」）→ Skill 门控本地工具（`SkillRuntime.resolveSkillGatedTools`，必须随基础工具注册否则激活后报 not a valid tool）；两处「工具名冲突」文案逐字。能力差异：`asyncio.gather` 并发 → 同顺序串行（结果集合与顺序一致，仅耗时不同）。至此 service.py 166 行全量落地）

## 四、storage 后端（3，minio 已搬）
参考路径前缀 `package/<ref>/storage/`
- [x] postgres/manager — postgres/manager.py（Spring/MyBatis 接管，标记为 N/A）
- [x] neo4j/manager — neo4j/manager.py（storage/neo4j/Neo4jConnectionManager，`688abda`）
- [x] redis/manager — redis/manager.py（Spring StringRedisTemplate 接管，标记为 N/A）
> 注：`storage_migrations/`（5）由 actable/Spring 接管，可不做。

## 五、server/API 层（33）
参考路径前缀 `package/../server/`（即 `backend/server/`）
### 入口（2）
- [ ] main — main.py（FastAPI app 入口 + 路由装配；WenquServerApplication 已存在但无路由）
- [ ] worker_main — worker_main.py（ARQ worker 入口）
### routers（25）
- [x] agent_invocation_call_router — routers/agent_invocation_call_router.py（AgentInvocationCallController；`/api/agent-invocation/agent-call`，2 端点：创建运行（`async_mode` 双分支 —— 异步立即回 run_id、同步阻塞等终态，`dispatched` 对外报 `pending`）+ 读取 OpenAI-compatible 结果（可带 `agent_slug` 做归属校验，不符 409）。**只做 HTTP 协议适配**：Conversation/Request/Run 一律交给 `submitAgentRequest`）
- [ ] agent_invocation_channel_router — routers/agent_invocation_channel_router.py
- [x] agent_invocation_eval_router — routers/agent_invocation_eval_router.py（AgentInvocationEvalController；`/api/agent-invocation/eval`，1 端点：创建评估运行并阻塞等终态，可选 `include_trajectory_summary`。轨迹摘要逐字照搬 `_build_trajectory_summary`（`tool_key` 的 started/finished 归并 + `open_tools` fallback 队列 + `schema_version=1`）。**摘要失败只 `log.warn`，不影响主结果**）
- [x] agent_router — routers/agent_router.py（AgentController；`/api/agent`，**23 端点**：后端清单 2（列表/详情）+ 智能体定义 7（列表/默认/创建/详情/更新/删除/设默认）+ 运行与请求队列 14（创建运行 resume 与 chat 两分支 / 请求详情 / 线程队列快照 / 继续队列 / 取消 / 抢占 / 请求事件流 SSE / 运行详情 / 运行结果 / langfuse 链接（超管）/ 取消运行 / 运行事件流 SSE / 线程活动运行）。**pydantic 契约落为控制器层 422**：`AgentCreate`/`AgentUpdate`/`AgentRunCreate` 三个模型本身只做类型声明、无 Field 约束，故不造 422 形状；但 `AgentUpdate` 的 `model_fields_set` 语义（显式传 `description`/`icon` 为 null = 清空）与 `AgentCreate.set_default` 的 422 逐字保留。**新增 `AgentBackendInfoProvider`** 把 `AgentRepository.BackendInfoProvider` 扩展点接到 `AgentManager`（对应参考实现 `serialize` 里的 `from yuxi.agents.buildin import agent_manager`）——不接则 `GET /api/agent` 的 capabilities/metadata 恒空（静默降级）。两条 SSE 经 `StreamingResponseBody` 直写服务层已生成的帧，响应头 Cache-Control/Connection/X-Accel-Buffering 逐字对齐）
- [x] auth_dept_router — routers/auth_dept_router.py（DepartmentController；`/api/departments`）
- [x] auth_router — routers/auth_router.py（AuthRouterController；`/api/auth`，与既有 `/api/ai/auth` 并存。22 个端点：token/cli 会话 4 个/check-first-run/initialize/me/profile/users 4 个/access-options/validate-username/check-uid/upload-avatar/impersonate/oidc 4 个）
- [ ] chat_router — routers/chat_router.py
- [x] dashboard_router — routers/dashboard_router.py（DashboardController；`/api/dashboard`）
- [x] external_kb_router — routers/external_kb_router.py（ExternalKbController；`/api/knowledge/databases/external*`，5 端点：列出可见库 / 文件列表 / 检索 / 打开文件 / 文件内查找）
- [x] filesystem_router — routers/filesystem_router.py（FilesystemController；`/api/viewer/filesystem`）
- [x] graph_router — routers/graph_router.py（GraphController；`/api/graph`）
- [x] knowledge_dashboard_router — routers/knowledge_dashboard_router.py（KnowledgeDashboardController；`/api/dashboard/stats/knowledge`）
- [x] knowledge_eval_router — routers/knowledge_eval_router.py（KnowledgeEvalController；`/api/evaluation`，11 端点：数据集 upload/list/detail/download/delete/generate/resume + 评估 run 发起/历史/结果/删除）
- [x] knowledge_router — routers/knowledge_router.py（KnowledgeBaseController **全量重写至新知识库宇宙**（`knowledge_bases` 表 + KnowledgeBaseRepository/Manager/Runtime + KnowledgeResponseSerializer），**56/56 端点**（26 GET + 23 POST + 4 PUT + 3 DELETE；脚本对拍参考 56 = Java 56，缺失 0 / 多余 0）。**清单原记「57 个端点」为早期误计，实为 56**，已同步修正类 Javadoc 与 KnowledgeRouteSupport 注释。参考实现单文件里的 9 个模块级函数 + 5 个常量提取到 `service/KnowledgeRouteSupport`（与 ExternalKbController 共享，避免错误码分叉）；mindmap/sample-questions 的 DB+LLM 高点由 `knowledge/KnowledgeContentService` 承载。差异项：①`agent_manager.reload_all()`（§三等 agents 运行时，未搬）不调用——本工程运行时按 kbId 懒解析，无陈旧缓存；②`export` 在参考实现是基类空实现、Milvus 未覆写 → 映射 501；③SSE 用 `StreamingResponseBody` 手写 `data: {json}\n\n` 帧；④前端 `upload-folder`/`process-folder` 在参考后端**不存在**（前端死代码），未实现）
- [x] mcp_router — routers/mcp_router.py（McpController；`/api/system/mcp-servers`，10 端点：列表（普通用户脱敏 5 字段）/新建/详情/更新/删除/连通性测试/启用开关/工具清单/工具刷新/单工具开关；错误码 400/403/404/422/500 与文案逐字对齐，`extra="forbid"` → 422）
- [x] mention_router — routers/mention_router.py（MentionController；`/api/mention`）
- [x] model_provider_router — routers/model_provider_router.py（ModelProviderController；`/api/system/model-providers`，9 端点：列表 / 新建 / 详情 / 更新 / 删除 / 远端模型拉取 / 缓存刷新 / 分组模型 v2 / 连通性状态）＋ providers 数据面全量（`models/providers/{service,cache,builtin,repository}.py` → ModelProviderService / ModelProviderCache / BuiltinProviders（25 家）/ ModelProviderRepository，加 `models/{chat,embed,rerank}.py` 的 spec 选择与连通性测试面 → ModelSelectors；**25 家内置供应商逐字对齐，含注释掉未启用的 anthropic/google 条目亦未收录**）
- [x] project_router — routers/project_router.py（ProjectController；`/api/projects`）
- [x] scheduled_agent_router — routers/scheduled_agent_router.py（ScheduledAgentController；`/api/scheduled-tasks`，5 端点：列表/创建/更新（PATCH）/立即运行/删除。**pydantic 契约逐条落为控制器层 422**：① `extra="forbid"` 显式实现（多余键 → `extra_forbidden`；Jackson 默认忽略未知字段，不拦即静默通过，与参考相反）② `min_length`/`max_length`/`pattern` 各用独立错误码（`string_too_short`/`string_too_long`/`string_pattern_mismatch`）③ `ScheduledAgentUpdate.reject_null_enabled` 照搬——显式 `enabled: null` → 422，不拦会被服务层 `truthy(null)` 静默解释为「停用任务」④ `model_dump()`（全字段）与 `model_dump(exclude_unset=True)`（仅显式传入键）两种入参形状分别对齐 create/update）
- [x] skill_router — routers/skill_router.py（SkillController；参考实现两个 router（`/system/skills` + `/skills`）合并为一个类（类级无 `@RequestMapping`，逐方法声明完整路径，避免 Spring 多前缀凭空生成路径），对外即 `/api/system/skills` 与 `/api/skills`。**26 端点逐一对应**前端 `web/src/apis/skill_api.js` 的 26 处调用（路由集合对拍 0 差异）；响应体 `{"success":true,"data":…}`（非本产品 ResultJson），卡片/管理列表加 `allowed_access_levels`、草稿确认与批量删除加 `summary`；错误码照搬 `_raise_from_value_error`（含「不存在/无权」→404，否则 400）；请求体必填 → 422、字段级校验 → 422（`loc=["body",字段]`）、**空串放行**（`content:""` 保存空文件、`path:""`/`source:""` 由服务层判 400））
- [x] system_router — routers/system_router.py（SystemController；`/api/system`）
- [x] system_task_router — routers/system_task_router.py（TaskController；`/api/tasks`）
- [x] tool_router — routers/tool_router.py（ToolController；`/api/system/tools`，2 端点）
- [x] user_router — routers/user_router.py（UserController；`/api/user`）
- [x] workspace_router — routers/workspace_router.py（WorkspaceController；两个同前缀 router 合并为 `/api/workspace`，含 knowledge 只读三端点）
### server/utils（6）
- [x] access_log_middleware — utils/access_log_middleware.py（config/AccessLogFilter）
- [x] auth_middleware — utils/auth_middleware.py（config/AuthGuards）
- [x] common_utils — utils/common_utils.py（setup_logging → resources/logback-spring.xml）
- [x] knowledge_permissions — utils/knowledge_permissions.py（permissions/KnowledgePermissions）
- [x] knowledge_response — utils/knowledge_response.py（common/KnowledgeResponseSerializer）
- [~] lifespan — utils/lifespan.py（**启动组件已搬 5/11**：`builtin_mcp_servers`（config/McpStartupInitializer，required=false）、`builtin_skills` + `default_agents` + `model_providers` + `model_cache`（config/StartupDataInitializer，四者 required=true 按原顺序串行）+ `ensure_options_in_db`（config/OptionStartupInitializer，参考实现里是裸调用）＋`app.state` 两字段（config/StartupState）。**未搬 3 项**：`security_secrets`（AuthUtils.requireSecuritySecrets 已就绪，但接上会新增「必须配置 JWT_SECRET_KEY / API_KEY_DERIVATION_SECRET / SANDBOX_PROVISIONER_TOKEN 三个 ≥32 位且互不相同的密钥」这一启动前置——参考实现即如此，是否照搬待定）、`knowledge_base`（参考实现按 kb_type 预建共享执行器并在类型不受支持时 fail-fast；本工程 KnowledgeBaseFactory 有 `isTypeSupported`，但运行时是「单一通用执行器按 kbId 懒解析」，无逐类型实例可预建，等价实现方式待定）、`sandbox_provider`（本工程未部署 provisioner 沙盒，无对应实现）。另有 run 队列 worker 进程（services/run_worker.py）未搬，故 `/api/system/ready` 的 `worker` 检查恒为 error → 整体 503，与参考实现在无 worker 时行为一致）

---

## 已落地（本会话前，供追溯，不计入待办）
- 批次①~⑳：services 层 29/43（见上「已完成」）+ 基础设施（repositories/models/config/permissions/workspace 前置/common/agents 前置/knowledge 解析面/storage-minio）
- 详见 `.workbuddy/memory/2026-09-19.md` 逐批记录

## 移植记录
| 日期 | 条目 | commit |
|------|------|--------|
| 2026-09-19 | （清单建立，无新增搬移） | — |
| 2026-09-19 | RAGFlow 分块家族 9/12：dispatcher/nlp/presets/book/general/laws/qa/separator/md_parser_utils/semantic_utils | `ec74d88` |
| 2026-09-19 | RAGFlow 分块家族补齐 3/12 + 预设真正分流：semantic/table_utils + ChunkPresets.mapToInternalParserId 修正 | `9299405` |
| 2026-09-19 | DocumentService 接入 RAGFlow 预设打通闭环 + RagflowNlp 正则 `{,2}`→`{0,2}` 方言修复 | `dddf719` |
| 2026-09-19 | knowledge 根核心 9/9（base/manager/factory/runtime/read_models/schemas/preview/cache/security）+ implementations/milvus/read_only_connectors + KnowledgeTaskService | `77bbe8f` |
| 2026-09-19 | §五 批次一：server/utils 5/6（access_log/auth_middleware/common_utils/knowledge_permissions/knowledge_response）+ routers 7（mention/project/system_task/knowledge_dashboard/dashboard/graph/filesystem）+ 平台差异载体（GlobalExceptionHandler/ApiHttpException/AuthGuards/CorsConfig/LoginRateLimitFilter） | `bac62c1` |
| 2026-09-19 | §五 批次二：routers 3（user/auth_dept/system）+ storage/MinioUploads（upload_image_to_minio）+ config/LogPaths + config/StartupState + common/AppVersion + info.template.yaml | — |
| 2026-09-19 | §五 批次五：routers 1（tool）+ tools/service 数据面 | `a75818c` |
| 2026-09-19 | §五 批次四：routers 1（external_kb） | `62608ad` |
| 2026-09-19 | §五 批次三：workspace_service（9/9 函数）+ auth_router（AuthRouterController 22 端点 + CLI 会话/OIDC/头像上传）+ workspace_router（WorkspaceController 含 knowledge 只读三端点） | `20015b5` |
| 2026-09-19 | §五 批次六：model_provider_router + providers 数据面全量（ModelInfo/BuiltinProviders/ModelProviderCache/ModelSelectors + ModelProviderService 重写 + ModelProviderRepository 补全）；`_normalize_payload` 跨语言对拍 26/26 逐字一致 | `055b57f` |
| 2026-09-19 | 清单滞后项修正（graphs 6 / utils 2 / storage 3 实为已搬） | `55993b0` |
| 2026-09-19 | 批次七（部分）：`knowledge/eval/metrics.py` → EvalMetrics + common/LooseJson；**修正批次六签名偏差** `ChatAdapter.call(message, stream=False)`（参考实现 message 可为 str 或消息列表，返回 GeneralResponse.content） | `766cf2b` |
| 2026-09-19 | §五 批次七（续）：**检索面 aquery 补齐**（`KnowledgeBaseRuntime.aquery`：合并 kwargs→final_top_k/similarity_threshold/search_mode/use_reranker/use_graph_retrieval/recall_top_k/file_name 过滤，vector（VectorStore 超采样 + 内存 kb/file 过滤）/keyword（`KnowledgeChunkRepository.searchByKeywords`）/hybrid（0.7:0.3 加权融合）+ 图谱融合 + hydrateChunkSources + 重排；必要替换与能力差异均已在类注释标注）→ `KnowledgeBaseManager.aquery/retrieve` 改为委托；`knowledge/eval` 3 模块全搬（EvalBenchmarkGeneration / EvalEvaluator / EvalService）+ `EvalTaskService`（dataset_generation / rag_evaluation 两个 Durable Task Handler）+ `EvaluationRepository` 补 `getDatasetForUpdate`/`getRunForUpdate`；`knowledge_eval_router` → KnowledgeEvalController（`/api/evaluation`，11 端点，`{"message":"success","data":...}` 响应体 + `require_evaluation_dataset_read/manage` 依赖等价物） | `0477f43` |

| 2026-09-19 | §五 批次八：`agents/mcp/service.py` 全量 → McpService / McpTool / McpClientBundle / McpServerViews（+ MCPServerRepository 逐条对位、启动组件 McpStartupInitializer ← lifespan 的 `builtin_mcp_servers`）→ **解锁 `mcp_router`** → McpController（10 端点）。跨语言对拍：驼峰化 / Python 风格 JSON dumps / 配置哈希 29/29 行逐字节一致；路由对拍 10/10；mcp_servers 造行 + 三条查询语义 + ensure_builtin 三步终态 + 清理（剩 0 行） | `fa3f111` |
| 2026-09-19 | §三 批次九：`agents/skills/service.py`（1827 行，90 函数 90/90）+ `agents/skills/remote_install.py` + 内置 skill 资源（`src/main/resources/skills/` 5 技能 9 文件）→ **解锁 `skill_router`** → SkillController（26 端点，两个参考 router 合并为一个类）。路由集合对拍 26/26 零差异；函数级覆盖对拍 90/90（唯一差异项 `_user_skills_file_lock` → 回调式）；内置清单/常量逐字对齐 | `2916c3f` |
| 2026-09-19 | **首次实跑（服务真起 + 登录 + 打接口）**：修 7 处只在运行期成立的缺陷（PosixPathLite.join 空基越界 → initBuiltinSkills 必抛；javac 残留 .class 致 Mapper bean 冲突；同名 Mapper 跨包 → 全限定 bean 名生成器；MilvusGraphService/TaskService 多构造器缺 @Autowired；Neo4jAutoConfiguration 强制建 Driver；UserContextInterceptor 只认单一令牌契约致参考契约全 401；KnowledgeBaseController 缺 `/api` 前缀）。补齐启动组件至 5/11，运行期派生目录进 .gitignore。冒烟 16/16 + 读写往返 12/12 通过 | `ef528ce` |
| 2026-09-19 | §二 批次⑩：`utils/mindmap_utils.py` + `utils/sample_question_utils.py` 的**高层 DB/LLM 面** → `knowledge/KnowledgeContentService`（10 + 2 函数）→ **解锁 `knowledge_router`** → `KnowledgeBaseController` 全量重写至新知识库宇宙（`knowledge_bases` 表 + Repository/Manager/Runtime）**56/56 端点**；参考实现 9 个模块级函数 + 5 个常量 → `service/KnowledgeRouteSupport`（与 ExternalKbController 共享）。端点集合对拍 56==56 零差异；全量编译 343 源文件 0 错误；实跑冒烟 10/10 只读端点 + 错误码通过（422/404/401/501 均符合预期），建库 400 系参考实现 `manager.py:507` 的 `if not embedding_model_spec: raise` 语义，与参考一致（非缺陷） | `5a81263` |
| 2026-09-19 | §三 批次⑪：`agents/base.py` → **BaseAgent**（含 4 个模块级纯函数）+ `backends/knowledge_base_backend.py` → KnowledgeBaseBackend；新增承载类 `ModelDumpable`/`ToolMessage`/`GraphCommand`；`BaseContext` 补 `setDynamic`/`getDynamic`（对应 `setattr/getattr` 非声明字段，`set()` 只写声明字段会静默失效）；图引擎收敛为 `AgentsGraphPort` 端口（无实现）。**核查发现 §三 整体阻塞于 `langgraph`+`deepagents`+`langchain_core` 三套缺失框架**，已写入清单专项说明 | `38ecdaf` |
| 2026-09-19 | §一+§三 批次⑫：`agent_run_service.py` 全量 → AgentRunService（+ `ArqPort` 端口、`AgentRunCreationScope`/`AgentRunWaitTimeout`）；agents 运行时数据面三件套 `agents/context.py` → AgentContextService、`agents/skills/runtime.py` → SkillRuntime、`agents/buildin/__init__.py` → AgentManager；`common/ApiHttpException` 补便捷工厂、`BaseAgent.resolveContextSchema()`、`AgentRun.toDict()` | `ba0db81` |
| 2026-09-19 | §一 批次⑬：`agent_config_service.py` → AgentConfigService、`agent_run_manifest_service.py` → AgentRunManifestService；新增 `common/CanonicalJson`（`json.dumps(sort_keys=True, separators=(",",":"))` 的 Java 载体）；**功能性修正**：`AgentRunService.canonicalJson` 原用 fastjson（不排序键）致 resume 的 request_id 幂等失效，改走 CanonicalJson | `9d5a5d3` |
| 2026-09-19 | §一 批次⑭：`agent_request_queue_service.py` → AgentRequestQueueService（排队策略/steer/SSE/派发与恢复）、`agent_request_service.py` → AgentRequestService（RunOrigin + AgentRequestInput + submitAgentRequest）；`AgentRunRequestRepository` 补 `listQueuedScopes`/`updateQueueState`、`AgentRunRepository` 补 `listPendingDispatchScopes`、`AgentRunRequest.toDict()` | `93ae913` |
| 2026-09-19 | §一 批次⑮：`scheduled_agent_service.py` 全量 → ScheduledAgentService（21 函数：校验 / 幂等创建 / 更新 / 软删 / 立即运行 / 派发 / 失败结算 / 恢复 / 到期领取）+ 自实现 `common/CronSchedule`（croniter 等价，能力差异已标注）；`ScheduledAgentJob`/`ScheduledAgentRun` 补 `toDict()`，`ScheduledAgentRepository` 补 `getJobById`/`lockRun`/`updateJob`/`updateRun` 并让 `listRecentRuns`/`getRequestAndRun` 带出 `error_message`/`finished_at`（终端状态事实源），`UserRepository` 补 `lockActiveByUid`。**参考实现 test_scheduled_agent_service.py 断言对拍 19/19 PASS**，并以 Python 基线交叉核对 `buildRequestId` 摘要段与 `intentHash` 逐字节一致 | `4138d33` |

| 2026-09-19 | §一 批次⑯：`subagent_run_service.py` 全量 → SubagentRunService（`SubagentStartResult` / `SubagentRunBusy` 异常、`subagentRunUrls` / `serializeSubagentRunState`（键序 + null 过滤）、`start` 三阶段事务拆分、`getRunForCreator` / `createRunRecord` / `ensureChildConversation` / `validateThreadRelation` / `ensureThreadRelation`、`translateCreationError`）。**参考实现 test_subagent_run_service.py 契约对拍 16/16 PASS**，`hash_id` / `subagent_child_thread_id` 与 Python `hashlib` 基线逐字节一致。同时解除 `chat_service` 对该模块的 import 依赖 | `9ee0c06` |
| 2026-09-19 | §一 批次⑰：`artifact_service.py` 全量 → ArtifactService（artifact 下载 / 预览 / 保存工作区 6 函数）。依赖面 `FilePreviewService`/`WorkdirService`/`SkillService`/`SafeFiles`/`BackendPaths`/`Workspace` 全部就位、**无 langchain 依赖故可直搬**。**参考实现 test_artifact_service.py 契约对拍 28/28 PASS** —— 路径允许/拒绝矩阵、`Content-Disposition` 百分号编码三断言（attachment 前缀 / 无 CR·LF / **不含原始文件名**）、`copyArtifactToPath` 的 404/400/403/413 映射、有界复制字节一致。全量编译 361 源文件 0 错误 | `0d8a1ba` |
| 2026-09-19 | §三 批次⑱（**引擎底座，非照搬项**）：以 spring-ai-alibaba 的 graph-core / agent-framework 为底座，把参考实现消费的 LangGraph 图 + `create_agent` ReAct 循环 + 中间件挂载点 + checkpointer + `astream_events("v3")` 事件词汇桥接为 `agents/engine/`：`GraphFactory`（`create_agent` 等价入口）/ `GraphPort`（`astreamMessages`/`astreamEvents`/`ainvoke`/`agetState`/`updateState`）/ `AgentEventStream`（事件词汇合成，数据源为节点输出的 `StreamingOutput.message()`）/ `GraphCodec`（承载类 ↔ 框架消息/配置/状态互转）+ 承载类 `AIMessage`/`GraphStateSnapshot`；`BaseAgent.agetState` 改返回快照载体。**类名按职责命名，不含实现框架名**。能力差异 5 类逐条标注（默认 MemorySaver 致 `hasCheckpointer` 恒真 / `recursionLimit` 编译期 / tools 事件 input 恒空 / values 仅图末一次 / 无 checkpoint 的 thread `updateState` 显式抛错）。验证：367 源文件 javac 0 错误；stub 模型端到端验证台 **40/40 PASS** | `eae0a77` |
| 2026-09-19 | §三 批次①（middlewares 第一批 5/10）：`context`→`ContextAwareInterceptor`（合并 @dynamic_prompt+@wrap_model_call）、`steer`→`SteerMiddleware`（`@HookPositions({BEFORE_MODEL,AFTER_MODEL})`、`canJumpTo=[end]`、读 metadata.context.run_id → `{jump_to:"end"}` 与框架 ReactAgent 读 state.jump_to 逐字同形）、`dynamic_tool`→`DynamicToolMiddleware`（工具筛选 + `EMPTY_TOOL_SENTINEL` 兜底框架"空列表=不过滤"差异、McpTool 适配 ToolCallback）、`network_retry`→`NetworkRetryMiddleware`（网络预算退避 + 非网络次数双轨）、`memory`→`MemoryMiddleware`（3 受限工具 + ThreadLocal 模拟 ToolRuntime）。**NetworkRetry 父类 `ModelRetryMiddleware`（langchain 1.3.17）语义逐字对齐**：`max_retries`=初始调用之后的重试次数(总=max+1)、`on_failure` 默认 continue(返回错误 AIMessage)/error(重抛)、退避 `initial*factor^n` 上限+±25% jitter、`ModelError.is_retryable` 对位 Non/TransientAiException、不可重试异常立即上抛不进 on_failure、状态码优先于文本。验证台 **51/51 PASS**（D 段 16 项对拍参考实现单测契约）；全量 373 源文件 0 错误 | `77ca2e3` |
| 2026-09-19 | §三 批次②（middlewares 第 6 个）：`token_usage`→`TokenUsageMiddleware`。**框架差距实证**：`wrap_model_call` 返回 `Command(update=...)` 直接写回 LangGraph state → 本工程 `ModelInterceptor` 无此能力，把快照构建抽成**纯函数** `buildSnapshot()` 经 request context 传递、由构图方落 state。usage 元数据从 `ChatResponse.getMetadata().getUsage()`（非 AIMessage.usage_metadata）。纯函数聚合数学（分桶/累计/黑名单剔除/缓存命中/complete 判定）逐字对齐。验证台 `TokenUsageHarness` **37/37 PASS**（对拍 test_token_usage_middleware.py）；全量 374 源文件 0 错误 | `25a35c7` |
| 2026-09-19 | §三 批次⑥（`buildin/chatbot/{context,state}` + `buildin/subagent/context` → **ChatBotContext / ChatBotState / SubAgentContext**）：三个小数据类（17/59/15 行）直搬。**ChatBotContext** 在 `BaseContext` 上叠加 `subagents` 字段（list[str]|None，metadata name/options/description/type/kind 逐字），经 `declareExtraField` 构造期登记、不污染基类声明表；`subagents` 属「空集合语义」字段（null=全部可见、空列表=无），与 `AgentContextFields.EMPTY_ALL_CONTEXT_FIELDS` 一致。**SubAgentContext** 叠加 `parent_thread_id`（隐藏）+ `is_subagent_runtime`（隐藏，供 `SkillInstallTool` 拒绝子智能体安装）逐字。**ChatBotState** 全量照搬 `SubAgentRunState` 13 键序 + `status` 7 枚举序 + `merge_subagent_runs` reducer（按 run_id 合并、无 run_id 追加、不按工具调用 id/子线程 id 兼容匹配）；该 reducer 正对应 `SubAgentMiddleware` 的 `Command(update={"subagent_runs":[...]})`。**验证台 10/10 PASS**；全量 384 源文件 0 错误。两个 `graph.py`（110/154 行）仍挡在 `create_agent_composite_backend`/`create_agent_filesystem_middleware`/`sync_agent_context_skills`（未搬的 backends/skills），只搬常量与工具过滤口径，构图调用留接缝 | — |
| 2026-09-19 | §三 批次⑦（`callbacks/model_request_timing.py` → **ModelRequestTimingMiddleware**，`agents/callbacks/` 新包）：LangChain `BaseCallbackHandler` 的 Java 等价 = `ModelInterceptor`。首次 `interceptModel` 即「首次 ChatModel 调用前」，捕获 `DateTimeUtils.utcNowNaive()` 并经 `AgentRunRepository.recordFirstModelRequest(runId, workerId, observedAt, null)` write-once 落库（持久化层此前已落地却**从未被调用**，本类补上这个洞）。**关键平台差异**：参考实现是 per-run 实例（实例字段存首次时间），本工程中间件按既有约定为单例安全（无 per-run 可变实例字段），故把「已捕获」标志存进 `BaseContext` 动态字段 `_first_model_request_at`（per-run 天然隔离），避免单例串扰；仓储 `recordFirstModelRequest` 自带 write-once 守卫 + lease 约束（与参考「Run owner 持久化」一致）；写库异常 try/catch 仅 warning 不阻断 `handler.call`。**验证台 4/4 PASS**（对拍 `test_model_request_timing.py`：同 run 仅写一次 / 无 run_id 不写 / 仓储异常不阻断 / 时间戳非空）；全量 385 源文件 0 错误。**待接线**：需并入引擎底座 `GraphFactory`/`GraphPort` 的 `interceptors(List)` 链（与现有 middlewares 同批，当前拦截器链尚未实际连到运行图） | — |
| 2026-09-19 | §三 批次⑤（`toolkits/buildin/tools.py` → **BuildinTools**）：4 个内建工具全量 —— `web_search`（豆包链路 payload/响应解析/HTTP 15s 全量；Tavily 无实现故不注册）、`present_artifacts`、`ocr_parse_file`、`ask_user_question`。**发现 `utils/question_utils.py` 早已落地为 `common/QuestionUtils`**（清单从未列它，又一次「清单是意图不是事实」）。四个工具描述、六类校验文案、OCR 输出路径规则逐字对齐。能力差异：Tavily 供应商、沙盒文件存在性校验、OCR 解析（`services/ocr_service.py`）三处不可用，均保留纯函数校验 + 到不可用步抛错。**验证台 30/30 PASS**（豆包 payload 10 项、响应解析 3 项、OCR 路径与预览 6 项、提问归一化 7 项、沙盒作用域 4 项）。全量 381 源文件 0 错误 | `bb0218b` |
| 2026-09-19 | §三 批次④（toolkits 4/5）：`toolkits/service.py` 补齐 `resolve_configured_runtime_tools`（基础工具 + MCP + Skill 门控工具，两处「工具名冲突」文案逐字；`asyncio.gather`→同顺序串行）→ **ToolkitsService 166 行全量**；`toolkits/kbs/tools.py` → `KnowledgeTools`（7 个知识库工具，正是 knowledge-base 内置 Skill 的依赖工具，逐字对齐分类/标签/展示名与 4 个共享 helper）；`toolkits/buildin/install_skill.py` → `SkillInstallTool`（`_run_install_task` 全量 + 六类结果文案逐字）；`toolkits/debug/tools.py` **参考实现为空文件**，标记无需照搬。**KnowledgeTools 验证台 18/18 PASS**（7 工具顺序/展示名/schema 必填项、`_find_query_target` 三分支、`_resolve_download_output_path` 重名递增与目录穿越防护、7 条入参校验文案）。能力差异：`download_kb_file` 沙盒落盘与 `install_skill` 沙盒/远程安装均因 `backends/sandbox` 未搬不可用 —— 保留校验、到沙盒步抛错并落「安装异常：…」文案（同 dify/notion 口径）。剩余 `toolkits/buildin/tools.py`（web_search/present_artifacts/ocr_parse_file/ask_user_question）。全量 380 源文件 0 错误 | `9ca11e5` |
| 2026-09-19 | §三 批次③（middlewares 收官 4/4 → **10/10**）：`skills`→`SkillsMiddleware`（+ 内部类 `SkillActivationInterceptor`）、`model_input`→`ImageInputCompatibilityMiddleware`、`subagent_task`→`SubAgentMiddleware`、`summary`→`SummaryMiddleware`。三者共同沿用批次② 确立的 **Command 写回通用解法**（纯函数 / 载体 + 上下文传递，由构图方落 state）：skills 走 `ToolCallResponse` metadata、subagent_task 走 `ToolCommand` + `drainPendingCommands()`、summary 走 `drainStateUpdate()`。**SkillsMiddleware 验证台 19/19 PASS**（对拍 test_skills_middleware.py：effective skills 提示、preloaded 单次注入、门控工具显隐、read_file 激活与越权拒绝、个人 Workspace 路径、位置/列表段格式、reducer 去重保序）。必要替换与能力差异逐条标注在各实现类注释；全量 378 源文件 0 错误 | `02e2e25` |
| 2026-09-19 | §三 批次⑧（backends 沙盒表面 port，能力差异口径）：`composite.py`→`AgentCompositeBackend`、`sandbox/download.py`→`SandboxDownload`、`sandbox/provisioner_client.py`→`SandboxProvisionerClient`+`SandboxRecord`；新增 `backends/sandbox` 包（`SandboxFsBackend` 接口 / `ProvisionerSandboxBackend` 最小标记 / `SandboxProvisionerException`）。deepagents.CompositeBackend/FilesystemMiddleware 未照搬、沙盒 provisioner 未部署 → 框架相关构造与 ls/download 统一抛异常（绝不静默成功）。补正清单：批次⑫ 已搬的 `skills/runtime` 曾被误标 `[ ]`。**SandboxDownload 验证台 17/17 PASS**（对拍 test_sandbox_download.py） | `ab6e006` |
| 2026-09-19 | §三 批次⑨（sandbox 全量）：`backends/sandbox/backend.py` → **ProvisionerSandboxBackend**（~1000 行全量直译：纯逻辑 / 权限 / 结果塑形 / 安全脚本生成 / 错误映射逐字对齐；I/O 动词经 `SandboxRuntimeClient` 接口隔离，未部署时 `buildClient` 抛 `SandboxProvisionerException`，绝不静默成功）+ `backends/sandbox/provider.py` → **ProvisionerSandboxProvider**（~290 行：token/SHA-256 沙盒 id/_sandbox_key/normalize_env/loadUserAgentEnv→AgentEnvRepository/单例 holder；弱引用 GC 语义标注为能力差异）+ 配套 `SandboxConnection`/`SandboxIdentityMismatchError`/`SandboxRuntimeClient` + 4 错误类型。AgentCompositeBackend.createBackend 接入 sandboxProvider。全量编译 400 源文件 0 错误 | `e8d7d9d` |
| 2026-09-19 | §三 批次⑩（`backends/composite.py` 收尾 → **真接线**，composite 勾选）：**框架等价构件核查**发现引擎面（spring-ai-alibaba agent-framework 1.1.2.3）自带三件等价物 —— `ToolInterceptor`（＝ `wrap_tool_call`）、`LargeResultEvictionInterceptor`（＝ `_intercept_large_tool_result`，其注释自述 "from Python's FilesystemMiddleware.wrap_tool_call"）、`FilesystemBackend`（backend 协议）；故批次⑧ 的「能力差异口径」在此收窄为真实现。落地 ① `YuxiFilesystemMiddleware`（参考实现原名，类名去品牌前缀）→ **FilesystemMiddleware** 提为顶层 `ToolInterceptor`：`interceptToolCall` 逐行复刻 `wrap_tool_call` 分支序（取结果 → 豁免集放行 → 预算开关放行 → 裁剪），裁剪委托框架裁剪器时把下游 handler 换成「返回已算好的结果」以**避免工具重复执行**；豁免集 = 框架 `excludeFilesystemTools()` 声明的 6 项（ls/read_file/write_file/edit_file/glob/grep，不含 execute）∪ `open_kb_document`（deepagents 私有常量 `TOOLS_EXCLUDED_FROM_EVICTION` 源码不可得，改用框架对同一 Python 行为的声明集，类注释已标注）。② 新增 `SandboxFilesystemBackendAdapter implements FilesystemBackend`：结构化 DTO → 字符串协议投影；**框架 `LARGE_RESULTS_DIR` 硬编码 `user.dir/large_tool_results/` 且 `processLargeResult` 私有不可覆写 → 在 I/O 边界重映射到 `<artifacts_root>/large_tool_results/`**（不重映射则写入落在沙盒授权根外，退化成「有裁剪消息、无落盘」的静默降级）。③ `_BackendScope` 补 `artifactsRoot()` 与 `AgentCompositeBackend.artifactsRoot(context)` 显式出口（参考实现从 CompositeBackend 实例读该值）；`createBackend` 返回具体类型。**`CompositeBackend` 未照搬确认无语义损失**（参考实现 `routes={}` 空路由表）。**`buildin/{chatbot,subagent}/graph.py` 的阻塞由此解除**。全量编译 402 源文件 0 错误 | `363626f` |
| 2026-09-19 | §三 批次⑪（`buildin/{chatbot,subagent}/graph.py` 构图装配 → **§三 待办清零**）：① **ChatbotAgent**：类属性 name/description/capabilities/context_schema 逐字；`get_graph` 逐行对位（`_runtime_prepared` 校验 → `sync_agent_context_skills` → `create_agent_composite_backend` → `load_chat_model(resolve_chat_model_spec(context.model))` → `resolve_configured_runtime_tools` → `build_prompt_with_context` → `_build_middlewares` → `_get_checkpointer`）；**middleware 一列拆 hooks(Steer) + interceptors（原序不重排）**：filesystem → skills(模型面+工具面) →（memory）→（subagent）→ summary → `TodoListInterceptor(systemPrompt=TODO_MID_PROMPT)` → `PatchToolCallsInterceptor` → `NetworkRetryMiddleware(model_retry_times)` → model_input → token_usage。② **SubAgentBackend** + **SubAgentToolFilterMiddleware**：类属性与 `get_info` 的 tools.options 过滤逐字（过滤用**基础集**——敏感工具仍列出、只在运行期隐藏/拒绝）；`get_graph` 构图期即用 `_filter_disabled_tools` 摘掉禁用工具；中间件列表**无 Steer/Memory**（与参考一致），hooks 为空、`NetworkRetryMiddleware()` 走默认。`_SubAgentToolFilterMiddleware` 因 Java 单继承（ModelInterceptor/ToolInterceptor 皆抽象类）拆为**模型面 + 工具面（`asToolInterceptor()`）两个注册对象**，工具面按 `ToolCallResponse.error` 回 `status="error"` 拒绝显式传入的禁用调用（工具列表隐藏不构成执行边界）。③ 配套：**AgentChatModel**（`resolve_chat_model_spec` 逐字；`load_chat_model` 调用面 —— 本工程无 `model_cache` 多模型注册表，故不做 spec 存在性/type 校验，已标注）、**AgentGraphSupport**（两文件重复的 5 行纯抽取）、**SummaryMiddleware.createFromContext**（对应 `create_summary_middleware_from_context`；generator/offloader 传 null，沿用既有能力差异 1 口径）、**AgentManager** 改由 Spring 装配实例（`reload=True` 对单例收敛为清 graph 缓存，已标注）。**三处能力差异显式标注**：① 审批中间件未注册（无 HumanInTheLoop 对位，仅保留 interrupt_on 形状 + normalize 边界校验）② 压缩事件通道未接线（`CompressionEventSink` 传 null）③ `state_schema` 无对应传参。全量编译 407 源文件 0 错误 | `b470197` |
| 2026-09-19 | §五 批次㉑（routers 首个真闭环批次，两项）：`routers/scheduled_agent_router.py` → **ScheduledAgentController**（`/api/scheduled-tasks`，5 端点）+ `routers/agent_router.py` → **AgentController**（`/api/agent`，**23 端点**；含「请求事件流」与「运行事件流」两条 SSE）。**pydantic 契约逐条落为控制器层 422（不静默）**：① `extra="forbid"` 显式实现（多余键 → `extra_forbidden`，Jackson 默认会放过）② `min_length`/`max_length`/`pattern` 各用独立错误码（`string_too_short`/`string_too_long`/`string_pattern_mismatch`）③ `ScheduledAgentUpdate.reject_null_enabled` 照搬（显式 `enabled:null` → 422，不拦会被 `truthy(null)` 静默停用任务）④ `AgentUpdate` 的 `model_fields_set` 语义（显式传 null = 清空）⑤ `model_dump()`（create 全字段）vs `model_dump(exclude_unset=True)`（update 仅显式键）两种入参形状分别对齐。**新增 `AgentBackendInfoProvider`**：把 `AgentRepository.BackendInfoProvider` 扩展点接到 `AgentManager`（对应参考实现 `serialize` 里的 `from yuxi.agents.buildin import agent_manager`）——未装配时 `GET /api/agent` 的 capabilities/metadata 恒空 = 静默降级，故必须接。SSE 用 `StreamingResponseBody` 直写服务层已生成的帧（`SseUtils.formatSse`），响应头逐字对齐。全量编译 410 源文件 0 错误 | `943423a` |
| 2026-09-19 | §五 批次㉒（routers 第二批真闭环，两项）：`routers/agent_invocation_call_router.py` → **AgentInvocationCallController**（`/api/agent-invocation/agent-call`，2 端点）+ `routers/agent_invocation_eval_router.py` → **AgentInvocationEvalController**（`/api/agent-invocation/eval`，1 端点 + 轻量轨迹摘要）。**新增 `common/PydanticValidation`**（pydantic 契约的控制器层落实现：必填 `missing` / `string_type` / `string_too_long` / `list_type` / `dict_type` / `model_type` / `bool_parsing`，错误体 `{"detail": [{"loc": [...], "msg", "type"}]}` 与 FastAPI 同形；**不含**默认的拒绝未知键 —— pydantic 默认 `extra=ignore`，只有显式 `extra="forbid"` 才拦）+ **`common/JsonValues`**（`text`/`truthy`/`or`/`asMap`，还原 Python 的 `str()` 与 `or` 真值语义）。**关键照搬点（易静默偏离）**：① `agent_slug` 必须 `strip`（参考单测断言 `" translator "` → `translator`）② `queue_policy` 默认值是 `async_mode` 二选一（`enqueue`/`reject`），且**同步 + 非 reject 必须 422**，漏掉会让同步调用被静默降级为排队；「空值回退」发生在 `strip` 之前，故 `"  "` 会 strip 成空串而非回退③ `agent_call_meta.context` 是**故意**拒绝的（防绕过显式运行上下文）④ OpenAI 兼容响应 `choices[0]` 的键名是 `messages`（参考实现的字面行为，非 OpenAI 惯例的 `message`），照搬勿「顺手修正」⑤ `status=="dispatched"` 对外报 `"pending"`⑥ `thread_id` 的 `max_length=64` **只有 eval 有**（call 没有），参考单测专测这条 ⑦ eval 的 `evaluation` 落 origin metadata 前要经白名单过滤（只留 `dataset_name`/`dataset_item_id`/`experiment_name` 且非空 strip），参考单测传 `{"ignored": "drop"}` 正是断言未知键被丢弃 ⑧ eval 的 `request_id` 从 `meta` 里取（call 从顶层取）⑨ `_normalize_usage` 入参是 `token_usage.total`（**dict**，非 int），仅当 `complete is True` 时才取；Python `isinstance(int)` 在 Java 侧映射为 `Integer|Long`（只认 `Integer` 会让大 token 计数静默归 0）⑩ 轨迹摘要失败**只告警不改主结果**（写成抛错会让一次成功评估因事件流读取失败整体 500）。**依赖面说明**：事件流**写入侧** `services/run_worker.py`（含 `{"items": buffer.items}` / `{"chunk": chunk}` 两种 payload 形状）属 §一 未搬项，读取侧 `RunQueueService.listRunStreamEvents` 已就位，故 `include_trajectory_summary` 当前读到空事件（零计数，非报错）。**已知重复（显式标注，待收敛）**：`JsonValues`/`PydanticValidation` 与 `AgentController`/`ScheduledAgentController` 内的私有同名工具重复，收敛属独立改动。全量编译 414 源文件 0 错误 | `ae7d8fe` |
| 2026-09-19 | §一 批次㉓（**§一 数据面收官**）：`services/chat_service.py`（1625 行）→ **ChatService**（2181 行）。**43/43 顶层函数全量落地**：输入装配 / Langfuse 上下文与 trace 固化 / Model+Tool 审计器 / artifact 路径归一 / agent_state 提取与签名 / token_usage 合并 / 流式事件翻译三件套 / 压缩事件载荷识别 / 消息落库（含 model·tool 审计对账）/ 中断三件套 / `resolveAgentRuntime` / `checkAndHandleInterrupts` / `streamAgentChat` / `streamAgentResume` / `serializeStateMessages` / `readCheckpointState` / `getAgentStateView`。**六处平台差异（类 Javadoc 显式标注）**：① async generator → `Consumer<String>` sink ② `asyncio.CancelledError` 无对位 ③ 显式 DB 会话 → `TransactionTemplate` ④ `model_request_recorder` 并入 `ModelRequestTimingMiddleware`（无 `_persist_model_request_timing` 调用点）⑤ checkpoint 读取改走「解析 Agent → 构图 → `agetState`」（无进程级 checkpointer 入口）⑥ 中断取值走 `GraphStateSnapshot.extractInterruptInfo()`。**品牌前缀必要替换**：`"yuxi.context_compression"` → `"wenqu.context_compression"`（生产者/消费者同处后端、`web/src` 零引用，与 `AgentRunService.AGENT_STATE_EVENT_NAME` 同口径）——producer（`SummaryMiddleware.CompressionEventSink`）与 consumer（`contextCompressionPayload`）两侧注释已互相指向，防止将来接线时字面量不一致导致压缩事件被静默丢弃。**配套补口**：`ModelMessageAuditCollector` 补 `runId()/requestId()/threadId()/workerId()`（参考实现以**属性** `model_audit.run_id` 暴露）；新增 `ChatService.copyMap`（Java 拷贝构造器不接受通配 `Map<?,?>`，而本类大量 `x instanceof Map<?,?> m` 后复制 ≡ Python `dict(x)`）。全量编译 415 源文件 0 错误 | `98bb13a` |
| 2026-09-19 | §一 批次㉔（主动上下文压缩）；`services/context_compression_service.py` → **ContextCompressionService**（7/7 函数）。逐行照搬：`compressThreadContext`（线程行锁 → idle 校验 → 可见性/后端/能力三级校验 → context 装配 → 一次性 runtime 内压缩 → 提交）、`ensureThreadIdle`（active_run / interrupted latest_run / queued 三态一律 409 `thread_busy`）、`compressAgentCheckpointInRuntime`（失败仍 release 且**不掩盖原错误**）、`ensureRuntimeAvailable`、`releaseRuntime`、`compressAgentCheckpoint`（`createFromContext(context, backend=composite backend)` 构建面照搬 + `AgentStateRepository` 读写）、`withCompressionUsage`（剔除 `TOKEN_USAGE_CONTEXT_FIELDS`、保留 `thread` 桶、追加 compression/summary_active/summary_trigger_tokens，阈值 = `summary_threshold * 1024`）。**两处能力缺口收敛为端口并显式抛错（绝不静默）**：`CheckpointCompressor`（`aforce_summarize` 骨架在未照搬的 langchain `SummarizationMiddleware` 基类）、`CheckpointStateWriter`（本工程 `AgentsGraphPort` 无 `aupdate_state` 对位）。另四处平台差异：无显式 DB 会话/commit 调用点（`TransactionTemplate` 圈边界）、`asyncio.to_thread` 同步直调、摘要阈值对非数值回落默认值、`_with_compression_usage` 空 update 跳过写入。**配套补口**：`BaseAgent.getCapabilities()` + `TOKEN_USAGE_CONTEXT_FIELDS` 提升为 public。全量编译 416 源文件 0 错误 | `615395b` |

## 挡在后面的依赖（routers 剩余 4 个的阻塞点）
> 依据：逐 router 提取 `from <ref>.…` 模块清单，与本工程已有类比对。**已落地**：`scheduled_agent_router`、`agent_router`（批次㉑）、`agent_invocation_call_router`、`agent_invocation_eval_router`（批次㉒）。剩余 `agent_invocation_channel_router` 另需 `chat_service`。（`knowledge_eval_router` 的检索面 `aquery`、`mcp_router` 的 mcp/service、`skill_router` 的 skills/{service,remote_install} 三个阻塞均已解除并搬完。）

| router | 阻塞项 | 归属 |
|---|---|---|
| knowledge_router | ~~mindmap_utils/sample_question_utils 的高层 DB/LLM 函数~~ **均已搬完（批次⑩ → KnowledgeContentService）→ knowledge_router 已解锁并全量落地（56/56 端点）** | §二 |
| scheduled_agent_router | ~~`scheduled_agent_service`~~ **已搬完（批次⑮）→ 已落地（批次㉑ → ScheduledAgentController，5 端点）** | §一 |
| chat_router | ~~`artifact_service`~~ **已搬完（批次⑰）**；~~`chat_service`~~ **已搬完（批次㉓ → ChatService，43/43 顶层函数）**；~~`context_compression_service`~~ **已搬完（批次㉔ → ContextCompressionService，7/7 函数）→ 阻塞清零**。**主动压缩专项（2026-09-19 核查，结论已落到实现）**：`context_compression_service._compress_agent_checkpoint` 调 `compressor.aforce_summarize(values)`，该方法的骨架全在 langchain 的 `SummarizationMiddleware` 基类（`_apply_event_to_messages` / `_determine_cutoff_index` / `_partition_messages` / `_aoffload_inline_media` / `_get_session_id` / `_build_new_messages_with_path` / `_count_tokens`），**本机无 langchain 源码 → 不可照搬**（框架自带的 `SummarizationHook` 语义不等价：无「历史落盘 + 可恢复」机制、cutoff 算法也不同，故不能替代）；另 `AgentStateRepository` 的 `StateGraphPort.updateState` 在 `AgentsGraphPort` 上尚无对应能力。该 service 的**外层**（线程锁 / idle 校验 / agent 与 capability 校验 / 模型解析 / workdir / runtime 生命周期 / token_usage 合并）不依赖上述缺失面，已独立落地；摘要链路与 checkpoint 写入按端口（`CheckpointCompressor` / `CheckpointStateWriter`）隔离，未装配时显式抛错而非静默降级。 | §一 |
| agent_router | ~~agent_config_service / agent_request_service / agent_request_queue_service / agent_run_service / agents/buildin~~ **§一 侧均已搬完（批次⑫⑬⑭ + AgentManager）→ 已落地（批次㉑ → AgentController，23 端点）** | §一 + §三 |
| agent_invocation_call_router | ~~`agent_request_service`~~ **已搬完（批次⑭）→ 已落地（批次㉒ → AgentInvocationCallController，2 端点）** | §一 |
| agent_invocation_channel_router | 上列（已解除）+ ~~`channel_command_service`~~（已搬）+ ~~`chat_service`~~ **已搬完（批次㉓）→ 阻塞清零，可直接搬** | §一 |
| agent_invocation_eval_router | ~~`agent_request_service` / `agent_run_service`~~ **均已搬完（批次⑫⑭）→ 已落地（批次㉒ → AgentInvocationEvalController，1 端点 + 轨迹摘要）**；事件流写入侧 `run_worker` 未搬，摘要暂为零计数（非错误） | §一 |
| skill_router | ~~`agents/skills/service.py` / `remote_install.py`~~ **均已搬完（批次九）→ skill_router 已解锁并落地** | §三 |

## §三 的根本阻塞与解法：第三方运行时框架（2026-09-19 核查 → 2026-09-19 底座已落地）

清点 §三 全部待办文件的 import 后确认：**§三 不是"把 Python 照搬成 Java"，而是把一整套
第三方 Agent 运行时框架重写到 Java**。参考实现依赖三套框架，本工程原本**一套都没有**：

| 依赖 | 用途 | 涉及 §三 条目 |
|---|---|---|
| `langgraph` | 编译图引擎：`CompiledStateGraph`（`astream`/`astream_events`/`ainvoke`/`aget_state`）、`Command`、`CustomTransformer`、checkpointer | `root/base.py`（已用 `AgentsGraphPort` 端口收敛）、`buildin/*`、全部 `middlewares/*` |
| `deepagents` | Agent 框架：`backends.CompositeBackend`、`middleware.filesystem.FilesystemMiddleware`（含 `FsToolName`/`TOOLS_EXCLUDED_FROM_EVICTION`）、subagent/summarization 中间件基类 | `backends/composite`、`backends/sandbox/*`、`middlewares/{skills,subagent_task,summary,model_input,dynamic_tool,memory,steer,context,network_retry,token_usage}`、`toolkits/*` |
| `langchain_core` | 消息类型（`ToolMessage` 等）、`BaseTool`、tool schema | `middlewares/*`、`toolkits/*`、`callbacks/*` |

**已照搬的部分只在"数据面"**：消息/状态/上下文/端口/纯函数/常量/文案，均逐字对齐。
**引擎面**（图执行、checkpoint、流式事件、中间件挂载）本批次改为**桥接而非重写**：
以既有 Java 原生等价框架（spring-ai-alibaba 的 graph-core / agent-framework）为底座，
收敛为参考实现所消费的端口与事件词汇（见上 `engine` 小节，`eae0a77`）。
本工程对框架面的既有口径仍是 **端口 + 显式标注的能力差异**（见 `BaseContext` / `AgentState` /
`AgentStateRepository.StateGraphPort` / `dify` / `notion` / `sandbox` / `skill_remote_install`），
`AgentsGraphPort` 继续沿用该口径 —— 只是现在它**有了实现**。

**结论（已决策并已落地底座）**：§三 剩余 27 项不能靠"逐文件直译"产出可运行代码，
需要先有 **Java 图引擎 + 中间件宿主**；该底座已就位，剩余条目的落地方式如下：

| 原依赖 | 剩余条目的落地方式 |
|---|---|
| `langgraph`（图执行 / checkpoint / 流式） | 用 `GraphFactory` 构图 + `GraphPort` 调用/取状态/订阅事件，无需自研引擎 |
| `langchain_core`（消息与工具类型） | 承载类（`AIMessage`/`ToolMessage`/`ModelDumpable`）与框架消息双向互转（`GraphCodec`） |
| `deepagents` 中间件 | 用 `Hook`（BEFORE/AFTER_AGENT、BEFORE/AFTER_MODEL）+ `ModelInterceptor`/`ToolInterceptor`；框架内另有 todo-list / 模型重试 / 人工审批等同类可对位 |
| `deepagents.backends`（文件系统 / 沙盒） | 落为本工程工具回调（`ToolCallback`）注册进 `GraphFactory`，路径面接 `BackendPaths` |

**下一步真正的工作量**：按上表逐中间件落地 `middlewares/*`（已完成 10/10）与 `buildin/{chatbot,subagent}/graph.py`
（两个 graph.py 的 backends 阻塞已在批次⑩ 解除，剩余为构图装配：`create_agent` → `GraphFactory`、
`TodoListMiddleware` → 框架 `interceptor/todolist`、`PatchToolCallsMiddleware` → `PatchToolCallsInterceptor`）
—— `chat_service` 已随批次㉓ 落地、`context_compression_service` 已随批次㉔ 落地（两个前置均已兑现）；§五 `chat_router` 的 §一 侧阻塞至此清零。
